# HANDOFF

## Назначение
Этот файл — единая точка передачи контекста для субагентов, чтобы продолжать работу без повторного чтения всего чата.

**Связанные документы:** чеклист старта — `START_CHECKLIST.md`; формат шага, ACTIVE, микрошаги — `docs/handoff-conventions.md`; старт чата, обзор, DoR/DoD — `AGENT_WORKFLOW.md`; политика оркестрации — `ProjectTP/subagent-orchestration-policy.md`; проверка контекста — `python scripts/check_agent_context.py`; дорожная карта продукта — `ProjectTP/docs/roadmap.md`.

## Правила обновления (обязательно)
- Записи в **Лог шагов** — по правилам **логического шага** и **микрошагов** в `docs/handoff-conventions.md` (не обязательно после каждой мелкой правки).
- Обязательные поля каждой записи (подробно и шаблон — `docs/handoff-conventions.md`):
  - **Задача**
  - **Время**
  - **Кто исполнил**
  - **Как исполнил**
- Записи должны быть короткими и фактологичными (3–6 строк).
- Смену статуса задачи и следующего шага фиксировать в **Лог шагов** (и при необходимости в `TASKS.md`); блоки **Текущий статус задачи** и **Следующий шаг** не переписывать без договорённости с пользователем (см. `CONTINUE_PROMPT.md`). Строку **ACTIVE** в блоке ниже **можно** обновлять при смене фокуса.

## Якорь ACTIVE
- **ACTIVE:** независимая генерация утренних/обычных дежурств — реализовано; миграция `a1b2c3d4e5f6`; см. шаг 179.

## Роли субагентов
- `orchestrator`: декомпозирует работу, назначает следующего исполнителя, снимает блокеры.
- `backend`: API, бизнес-логика, БД/миграции, happy-path проверки серверной части.
- `frontend`: UI, интеграция с API, состояние/рендеринг, happy-path проверки интерфейса.
- `designer`: UI/UX-аудит интерфейса, варианты визуального дизайна фронта и рекомендации по внедрению без изменения бизнес-логики.
- `devops`: Docker/compose, CI, env/секреты, воспроизводимость развертывания.
- `qa`: проверка acceptance criteria, happy-path тесты, дефекты и вердикт.

## Текущий статус задачи
- **Задача:** в корневом `TASKS.md` карточки **№49–№67** закрыты; открытых пунктов бэклога нет до пополнения.
- **Статус:** задачи **№1–№67** по текущему снимку репозитория закрыты.
- **Что сделано:** см. `TASKS.md` и **Лог шагов** ниже; дорожная карта — `ProjectTP/docs/roadmap.md`.
- **Ключевой результат:** продуктовый бэклог по состоянию на май 2026 исчерпан; дальнейшие изменения — через новые записи в `TASKS.md`.

## Следующий шаг
- **Кто:** владелец продукта / команда.
- **Действие:** при новых требованиях завести карточки в `TASKS.md` и назначить исполнителя.
- **Критерий готовности:** появление актуального бэклога и приоритизации.

---

## Лог шагов

### Шаг 1
- **Задача:** Изучить структуру репозитория и точки входа.
- **Время:** 2026-03-31 12:53 +03:00
- **Кто исполнил:** explore-subagent (structure)
- **Как исполнил:** проанализировал папки и файлы репозитория, docker и runtime точки входа, определил стек по исходным манифестам.
- **Результат:** определены ключевые директории приложения, стек, команды запуска и точки входа.

### Шаг 2
- **Задача:** Проанализировать архитектуру backend и риски.
- **Время:** 2026-03-31 12:53 +03:00
- **Кто исполнил:** explore-subagent (backend)
- **Как исполнил:** изучил основной backend-файл, связку compose/nginx, роуты и обработку конфигурации.
- **Результат:** составлена карта API и выделены риски по безопасности и архитектуре.

### Шаг 3
- **Задача:** Проанализировать frontend и базовое качество проекта.
- **Время:** 2026-03-31 12:53 +03:00
- **Кто исполнил:** explore-subagent (frontend/quality)
- **Как исполнил:** проверил структуру статического frontend, вызовы API, наличие тестов/линтеров/CI.
- **Результат:** подтверждено отсутствие автотестов/CI и предложены низкорисковые улучшения.

### Шаг 4
- **Задача:** Создать правила handoff для сабагентов.
- **Время:** 2026-03-31 12:53 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** свел результаты сабагентов в единый постоянный формат handoff и рабочие правила.
- **Результат:** внедрен процесс `HANDOFF.md` и зафиксировано стартовое состояние для продолжения.

### Шаг 5
- **Задача:** Запустить сабагентов для P1 №1 и определить минимальную реализацию.
- **Время:** 2026-03-31 13:39 +03:00
- **Кто исполнил:** orchestrator + backend/frontend/qa subagents
- **Как исполнил:** параллельно собрал план API, UI и checklist проверки для админских операций.
- **Результат:** согласован минимальный объем изменений без ломки текущего функционала.

### Шаг 6
- **Задача:** Реализовать backend операции админа по пользователям.
- **Время:** 2026-03-31 13:39 +03:00
- **Кто исполнил:** backend
- **Как исполнил:** добавил endpoint: `POST /api/admin/users/{id}/password`, `DELETE /api/admin/users/{id}/reports`, `DELETE /api/admin/users/{id}` в `backend/app/main.py`.
- **Результат:** сервер поддерживает смену пароля сотрудника, очистку истории отчетов и удаление сотрудника.

### Шаг 7
- **Задача:** Реализовать UI для админских операций и устранить блокирующий баг входа.
- **Время:** 2026-03-31 13:39 +03:00
- **Кто исполнил:** frontend
- **Как исполнил:** расширил `frontend/index.html` и `frontend/app.js` (селект сотрудника + кнопки операций + вызовы API); исправил синтаксическую ошибку `dataset.entry-index`.
- **Результат:** форма входа снова обрабатывается JS, а в админке доступны новые действия по сотруднику.

### Шаг 8
- **Задача:** Провести техническую проверку happy-path после внедрения.
- **Время:** 2026-03-31 13:39 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** выполнил синтаксические проверки (`py_compile`, `node --check`), перезапустил backend/proxy, прогнал скрипт `check_admin_ops.py`.
- **Результат:** проверка пройдена: login 200, create 200, change_password 200, delete_reports 200, delete_user 200.

### Шаг 9
- **Задача:** Спроектировать и реализовать P1 №2 (смена пароля пользователем).
- **Время:** 2026-03-31 14:08 +03:00
- **Кто исполнил:** orchestrator + backend/frontend/qa subagents
- **Как исполнил:** параллельно собрал минимальный API/UI/checklist, затем добавил `POST /api/me/password` и UI-блок смены пароля на `index.html`.
- **Результат:** задача №2 реализована end-to-end без изменения существующих потоков.

### Шаг 10
- **Задача:** Проверить happy-path смены своего пароля и очистить тестовые данные.
- **Время:** 2026-03-31 14:08 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** запустил `check_self_password.py` (create temp user -> login old -> change own password -> old fails -> new works -> cleanup delete).
- **Результат:** проверка успешна: `login_old_after_change=401`, `login_new_after_change=200`, тестовый пользователь удален.

### Шаг 11
- **Задача:** Вынести пользовательские настройки в отдельное окно и добавить редактирование профиля.
- **Время:** 2026-03-31 14:14 +03:00
- **Кто исполнил:** orchestrator + generalPurpose subagent
- **Как исполнил:** сабагентом подготовлен минимальный дизайн; внесены изменения в `frontend/index.html` (вкладка `Настройки`) и `frontend/app.js` (сохранение профиля + смена пароля в отдельной вкладке), добавлен `PATCH /api/me/profile` в backend.
- **Результат:** главная страница очищена от блока настроек, профиль пользователя редактируется в отдельной вкладке.

### Шаг 12
- **Задача:** Проверить новую логику обновления профиля и прокси-методы.
- **Время:** 2026-03-31 14:14 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** добавил поддержку `PATCH` в `local_dev_proxy.py`, перезапустил proxy/backend, прогнал `check_profile_update.py`.
- **Результат:** `PATCH /api/me/profile` работает (`200`), `GET /api/me` возвращает обновленное `full_name`.

### Шаг 13
- **Задача:** Реализовать P1 №3 (неактивные дежурные).
- **Время:** 2026-03-31 14:46 +03:00
- **Кто исполнил:** orchestrator + generalPurpose subagent
- **Как исполнил:** сабагентом сформирован минимальный план; внесены изменения в `backend/app/main.py`, `frontend/index.html`, `frontend/app.js` и `local_dev_proxy.py`.
- **Результат:** админ может включать/выключать участие сотрудника в генерации дежурств через UI.

### Шаг 14
- **Задача:** Проверить исключение неактивных сотрудников из генерации.
- **Время:** 2026-03-31 14:46 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** перезапустил backend, прогнал `check_inactive_duties.py` (создание 2 сотрудников, деактивация одного, генерация, проверка слотов).
- **Результат:** проверка успешна: `inactive_present: False`; неактивный пользователь не участвует в распределении.

### Шаг 15
- **Задача:** Реализовать P1 №4 (исправление скачивания Excel) через цикл сабагентов.
- **Время:** 2026-03-31 15:01 +03:00
- **Кто исполнил:** orchestrator + backend/frontend/qa/reviewer subagents
- **Как исполнил:** сабагентами проведены диагностика и QA-сценарий; внедрены backend и frontend фиксы; замечание ревьювера возвращено на доработку и исправлено в повторном цикле.
- **Результат:** итоговый ревью-вердикт PASS, smoke `check_excel_flow.py` пройден (owner download 200, guest 401, rebuild after missing file 200).

### Шаг 16
- **Задача:** Реализовать TASK №5 (realtime отображение дежурного).
- **Время:** 2026-03-31 15:06 +03:00
- **Кто исполнил:** orchestrator + generalPurpose subagent
- **Как исполнил:** добавил realtime блок в `frontend/index.html`, стили подсветки в `frontend/styles.css`, polling/timer-логику в `frontend/app.js`.
- **Результат:** данные графика и текущий дежурный обновляются автоматически без перезагрузки страницы.

### Шаг 17
- **Задача:** Прогнать цикл QA/ревью и доработок для TASK №5.
- **Время:** 2026-03-31 15:06 +03:00
- **Кто исполнил:** qa + reviewer subagents
- **Как исполнил:** QA дал PASS; ревьювер обнаружил блокер (затирание несохраненных правок админа), задача возвращена в доработку; внесен фикс `dutiesDirty`, повторный ревью PASS.
- **Результат:** блокирующая регрессия устранена, realtime-режим стабилен.

### Шаг 18
- **Задача:** Усилить переносимость контекста между чатами.
- **Время:** 2026-03-31 15:43 +03:00
- **Кто исполнил:** orchestrator + generalPurpose subagent
- **Как исполнил:** добавил обязательный startup-протокол в `ProjectTP/subagent-orchestration-policy.md` (чтение `HANDOFF.md`/`TASKS.md`/policy перед стартом) и создал `CONTINUE_PROMPT.md` (корень репозитория) с готовым шаблоном команды для нового чата.
- **Результат:** правила продолжения работы формализованы и закреплены в файлах проекта.

### Шаг 19
- **Задача:** Добавить правило обязательного обзорного сабагента при старте.
- **Время:** 2026-03-31 15:47 +03:00
- **Кто исполнил:** orchestrator + generalPurpose subagent
- **Как исполнил:** обновил `ProjectTP/subagent-orchestration-policy.md` (раздел 6.0 про предстартовый обзор всего проекта, запрет старта реализации без него, обновленный DoR) и `CONTINUE_PROMPT.md` (корень репозитория; пункт 0 с запуском обзорного агента).
- **Результат:** теперь в новом чате сначала запускается агент обзора проекта, затем выполняется стандартный порядок чтения `HANDOFF.md`/`TASKS.md`/policy.

### Шаг 20
- **Задача:** Выполнить обязательный предстартовый обзор всего проекта в новом чате.
- **Время:** 2026-03-31 15:52 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запустил отдельного обзорного агента по всему репозиторию и зафиксировал структуру, текущий фокус, риски и ближайшие шаги.
- **Результат:** подтвержден актуальный фокус на `P1 №8` (обмен дежурствами), выделены риски (монолитный backend, дублирующий контур `duty_app`, слабый тестовый контур).

### Шаг 21
- **Задача:** Синхронизировать старт выполнения `P1 №8` по startup-протоколу.
- **Время:** 2026-03-31 15:56 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** сверил `HANDOFF.md`, `TASKS.md` и `subagent-orchestration-policy.md`; подтвердил отсутствие конфликта источника состояния.
- **Результат:** выбрана целевая задача `P1 №8` с зафиксированным критерием готовности (self-swap запрет + входящее уведомление адресату).

### Шаг 22
- **Задача:** Реализовать backend/frontend flow запроса обмена дежурствами.
- **Время:** 2026-03-31 16:01 +03:00
- **Кто исполнил:** backend + frontend
- **Как исполнил:** в `backend/app/main.py` добавил модель `duty_swap_requests` и API `POST /api/duty-swaps`, `GET /api/duty-swaps/inbox`; в `frontend/index.html` и `frontend/app.js` добавил форму отправки запроса и блок входящих уведомлений с автообновлением.
- **Результат:** запрос обмена создается с сообщением вида "Ревин Сергей, Иванов Владимир запрашивает обмен дежурствами с 09:00 на 10:00"; self-swap блокируется на API.

### Шаг 23
- **Задача:** Проверить happy-path для `P1 №8`.
- **Время:** 2026-03-31 16:04 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** прогнал `py_compile` и `node --check`, перезапустил backend на актуальном коде, выполнил smoke `check_swap_request.py`.
- **Результат:** PASS — `create_swap_to_other=200`, `create_swap_to_self=400`, `inbox_status=200`, `inbox_count=1`.

### Шаг 24
- **Задача:** Закрыть функционал решения входящих запросов обмена (принять/отклонить).
- **Время:** 2026-03-31 16:10 +03:00
- **Кто исполнил:** backend + frontend + qa
- **Как исполнил:** в backend добавил статус запроса и endpoint решения `POST /api/duty-swaps/{id}/decision` с обменом слотов при `accept`; в frontend добавил кнопки `Принять/Отклонить` в списке входящих; обновил smoke `check_swap_request.py`.
- **Результат:** PASS — `accept_status=200 accepted`, после принятия назначения в слотах меняются местами (`after_accept_slot0_user`/`after_accept_slot1_user`).

### Шаг 25
- **Задача:** Выполнить обязательный обзор всего проекта перед продолжением работ.
- **Время:** 2026-03-31 16:10 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запустил отдельного обзорного агента по всему репозиторию, собрал краткую сводку по структуре, активным изменениям, рискам и следующему приоритетному шагу.
- **Результат:** подтвержден фокус на синхронизации источников состояния (`HANDOFF.md` vs `TASKS.md`) и приоритетах `P1 №14 -> P1 №11`; зафиксированы риски по runtime-артефактам и незавершенному realtime-flow.

### Шаг 26
- **Задача:** Синхронизировать старт выполнения `P1 №14` по startup-протоколу.
- **Время:** 2026-03-31 16:10 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** прочитал `HANDOFF.md`, `TASKS.md`, `subagent-orchestration-policy.md`; выбрал задачу `P1 №14` как следующий шаг из handoff.
- **Результат:** зафиксировано расхождение: в `TASKS.md` задачи `P1 №1/2/3/4/8` не помечены как выполненные, при этом `HANDOFF.md` содержит факт их реализации; предложено решение — вести статус выполнения в `HANDOFF.md` как источник истины и отдельным шагом синхронизировать `TASKS.md` после завершения `P1 №14`.

### Шаг 27
- **Задача:** Выполнить `P1 №14` (анализ справедливости генерации дежурств).
- **Время:** 2026-03-31 16:10 +03:00
- **Кто исполнил:** orchestrator + backend
- **Как исполнил:** проанализировал код генератора в `backend/app/main.py`, описал алгоритм и ограничения в `ProjectTP/duty_generation_fairness.md`, добавил воспроизводимый smoke `check_generation_fairness.py`, проверил синтаксис скрипта.
- **Результат:** задача №14 закрыта: fairness-логика задокументирована, пример проверки готов к запуску (`fairness_ok: delta <= 1`).

### Шаг 28
- **Задача:** Ввести правило обязательной отметки завершенных задач.
- **Время:** 2026-03-31 16:14 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** обновил `ProjectTP/subagent-orchestration-policy.md` (startup-протокол и список запретов), добавил требование помечать закрытую задачу в `TASKS.md` и синхронно обновлять `HANDOFF.md`.
- **Результат:** статус завершения теперь обязателен в обоих артефактах (`TASKS.md` + `HANDOFF.md`).

### Шаг 29
- **Задача:** Реализовать `P1 №11` (добавить слоты 07:00 и 08:00).
- **Время:** 2026-03-31 16:14 +03:00
- **Кто исполнил:** backend + frontend + qa
- **Как исполнил:** обновил диапазон слотов до `0..10` и стартовый час до `07:00` в `backend/app/main.py`, добавил миграцию SQLite-ограничений на слот, обновил frontend-константы/циклы в `frontend/app.js`, добавил smoke `check_morning_slots.py`.
- **Результат:** проверка пройдена на обновленном backend: `generate_status=200`, `slot_count=11`, `has_07_00=True`, `has_08_00=True`, `first_slot=07:00`, `last_slot=17:00`.

### Шаг 30
- **Задача:** Исправить неполное заполнение слотов и нечитаемую ошибку при сохранении графика.
- **Время:** 2026-03-31 16:27 +03:00
- **Кто исполнил:** frontend
- **Как исполнил:** в `frontend/app.js` убрал жесткую привязку к фиксированному числу слотов на клиенте: теперь UI берет фактический `slotCount` и стартовый час из ответа `/api/duties`; дополнил форматирование ошибок API, чтобы массив ошибок в `detail` отображался человекочитаемо.
- **Результат:** сохранение работает по фактически загруженным слотам (без ложной ошибки "не все слоты"), а вместо `[object Object]` показывается текст валидации backend.

### Шаг 31
- **Задача:** Протестировать ключевые happy-path сценарии и исправить блок "Сейчас дежурит".
- **Время:** 2026-03-31 16:31 +03:00
- **Кто исполнил:** frontend + qa
- **Как исполнил:** прогнал smoke `check_swap_request.py` и `check_excel_flow.py` (PASS), выявил отсутствие JS-логики для realtime-блока текущего дежурного; добавил вычисление текущего слота, рендер ФИО/слота и таймер автообновления в `frontend/app.js`.
- **Результат:** блок "Сейчас дежурит" теперь заполняется и подсвечивает активный слот на текущую дату; проверки обмена и Excel проходят.

### Шаг 32
- **Задача:** Выполнить `P1 №15` (аудит хранения паролей и Excel).
- **Время:** 2026-03-31 16:31 +03:00
- **Кто исполнил:** orchestrator + qa
- **Как исполнил:** проверил хранение паролей в SQLite (хеши bcrypt), повторно прогнал `ProjectTP/check_excel_flow.py`, зафиксировал выводы в `ProjectTP/security_audit_passwords_excel.md`; статус задачи отмечен как завершенный в `TASKS.md`.
- **Результат:** критерий `P1 №15` закрыт, дополнительных кодовых изменений по итогам аудита не требуется.

### Шаг 33
- **Задача:** Добавить и приоритизировать задачу "Excel только для админа + массовая выгрузка".
- **Время:** 2026-03-31 16:31 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** добавил новую задачу `№17` в `TASKS.md` с приоритетом `P1` и явным критерием готовности (admin-only download, единый архив, уведомление по несформированным).
- **Результат:** новая бизнес-задача формально зафиксирована в бэклоге и сразу взята в реализацию.

### Шаг 34
- **Задача:** Реализовать `P1 №17` (admin-only скачивание и массовая выгрузка Excel).
- **Время:** 2026-03-31 16:31 +03:00
- **Кто исполнил:** backend + frontend + qa
- **Как исполнил:** в `backend/app/main.py` ограничил `/exports/{filename}` ролью `admin`, добавил `GET /api/admin/reports/export-all`; в `frontend/index.html`/`frontend/app.js` добавил кнопку "Скачать все Excel" и уведомления по несформированным; обновил smoke `check_excel_flow.py` под новый контракт.
- **Результат:** проверка на обновленном backend: `download_as_support=403 Admin only`, `download_all_as_admin=200` (zip), массовое скачивание работает по требованиям.

### Шаг 35
- **Задача:** Проверить инцидент "утренние дежурства отсутствуют в новом графике".
- **Время:** 2026-03-31 16:46 +03:00
- **Кто исполнил:** qa + orchestrator
- **Как исполнил:** поднял актуальные backend (`:8000`) и proxy (`:8080`) на текущем коде, прогнал `check_morning_slots.py` через proxy.
- **Результат:** подтверждено, что в актуальном контуре слоты утренних дежурств присутствуют: `slot_count=11`, `has_07_00=True`, `has_08_00=True`.

### Шаг 36
- **Задача:** Реализовать `P2 №6` (n8n: уведомление за 2 минуты до дежурства).
- **Время:** 2026-03-31 17:12 +03:00
- **Кто исполнил:** backend + devops
- **Как исполнил:** в `backend/app/main.py` добавил endpoint `POST /api/admin/notifications/duty-upcoming/dispatch` и env-конфигурацию `N8N_WEBHOOK_URL`; в `ProjectTP/docker-compose.yml` добавил прокидывание `N8N_WEBHOOK_URL` через `.env`; добавил `ProjectTP/n8n_notification_workflow.md`.
- **Результат:** backend готов отправлять webhook-событие `duty_upcoming_2m` в n8n workflow.

### Шаг 37
- **Задача:** Проверить happy-path `P2 №6`.
- **Время:** 2026-03-31 17:12 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** перезапустил backend на актуальном коде и прогнал `check_n8n_notification.py`.
- **Результат:** PASS — `dispatch_status=200`, `start_time=07:00`, `sent=False`, `reason='N8N webhook is not configured'` (корректный dry-run без webhook).

### Шаг 38
- **Задача:** Реализовать `P2 №7` (редактирование сотрудников на одном экране).
- **Время:** 2026-03-31 17:14 +03:00
- **Кто исполнил:** backend + frontend
- **Как исполнил:** добавил `PATCH /api/admin/users/{user_id}` в `backend/app/main.py`; в `frontend/index.html` добавил таблицу редактора сотрудников; в `frontend/app.js` добавил API-вызов и inline-сохранение логина/ФИО с обновлением связанных селектов.
- **Результат:** единый экран сотрудников поддерживает просмотр и редактирование логина/ФИО без переходов.

### Шаг 39
- **Задача:** Проверить happy-path `P2 №7`.
- **Время:** 2026-03-31 17:14 +03:00
- **Кто исполнил:** qa
- **Как исполнил:** прогнал `check_user_inline_edit.py` (create -> patch username/full_name -> list verify -> cleanup delete).
- **Результат:** PASS — `patch_status=200`, обновленные `username/full_name` корректно возвращаются в `/api/admin/users`.

### Шаг 40
- **Задача:** Убрать кнопку "Сохранить черновик" и зафиксировать read-only после "Сформировать Excel".
- **Время:** 2026-03-31 17:18 +03:00
- **Кто исполнил:** frontend
- **Как исполнил:** удалил из `frontend/app.js` кнопку и обработчик `saveDraft`; после `finalize` добавил принудительное обновление списка отчетов из backend, чтобы карточка гарантированно рендерилась со статусом `final` и без редактирования.
- **Результат:** в UI осталась только кнопка `Сформировать Excel`, после ее нажатия редактирование отчета блокируется.

### Шаг 41
- **Задача:** Скорректировать поведение после "Сформировать отчет": разрешить дальнейшее редактирование и сохранять последнюю версию для скачивания админом.
- **Время:** 2026-03-31 17:22 +03:00
- **Кто исполнил:** backend + frontend + qa
- **Как исполнил:** в `backend/app/main.py` разрешил `PUT /api/reports/{id}` после финализации и добавил инвалидирование Excel-файла при изменении; в `frontend/app.js` сделал `Сформировать Excel` операцией "сначала сохранить entries, затем finalize" без блокировки полей; добавил smoke `check_finalize_editable.py`.
- **Результат:** подтвержден happy-path: после finalize можно править и снова формировать, в отчете фиксируется последняя сохраненная версия (`latest_entry_description=v2`) для последующей выгрузки админом.

### Шаг 42
- **Задача:** Выполнить обязательный обзор всего проекта перед продолжением работ.
- **Время:** 2026-03-31 17:39 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запустил отдельного обзорного агента по всему репозиторию и собрал краткую сводку по структуре, измененным файлам и следующим приоритетам.
- **Результат:** подтверждено текущее состояние (реализованные P1/P2 изменения и наличие runtime-артефактов), рекомендованный следующий шаг — `P2 №10` (push-блокировки и чек-лист разблокировки).

### Шаг 43
- **Задача:** Синхронизировать старт `P2 №10` по startup-протоколу и зафиксировать расхождение.
- **Время:** 2026-03-31 17:39 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** прочитал `HANDOFF.md`, `TASKS.md`, `subagent-orchestration-policy.md`, сверил приоритеты и критерий готовности.
- **Результат:** зафиксировано расхождение: в `TASKS.md` ранее выполненные задачи не были явно отмечены как закрытые; решение — отмечать статус выполнения в `TASKS.md` по мере закрытия задач и сохранять `HANDOFF.md` источником текущего состояния.

### Шаг 44
- **Задача:** Выполнить `P2 №10` (блокировка Push и чек-лист разблокировки).
- **Время:** 2026-03-31 17:39 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** подготовил документ `ProjectTP/push_unblock_checklist.md` с причинами блокировок, быстрой диагностикой, чек-листами для Chrome/Edge/Firefox и шагами эскалации в поддержку; отметил статус задачи в `TASKS.md`.
- **Результат:** критерий готовности `P2 №10` выполнен: причины задокументированы, чек-лист готов для публикации сотрудникам.

### Шаг 45
- **Задача:** Исправить регламент работы с `HANDOFF.md` после замечания пользователя.
- **Время:** 2026-03-31 17:48 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** зафиксировал правило append-only (история не переписывается), обновил `CONTINUE_PROMPT.md`, восстановил служебные блоки `Текущий статус задачи` и `Следующий шаг` к прежнему виду.
- **Результат:** правила продолжения уточнены; дальнейшая работа с `HANDOFF.md` — только дописывание новых шагов в конец файла.

### Шаг 46
- **Задача:** Выполнить `P2 №12` (терминология "дежурство" и формат времени `HH:00`).
- **Время:** 2026-03-31 17:54 +03:00
- **Кто исполнил:** frontend + orchestrator
- **Как исполнил:** в `frontend/index.html` заменил остаточные UI-надписи со "смены" на "дежурства"; в `frontend/app.js` изменил отображение слота с диапазона `07-08` на формат `07:00`; выполнил `node --check` и синхронизировал статус задачи в `TASKS.md`.
- **Результат:** критерий `P2 №12` закрыт: терминология унифицирована, время в графике показывается в формате `HH:00`, регрессий синтаксиса не выявлено.

### Шаг 47
- **Задача:** Зафиксировать переход к следующей задаче по приоритету.
- **Время:** 2026-03-31 17:54 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** после закрытия `P2 №12` сверил `TASKS.md`: `P2 №13` имеет внешнюю зависимость (шаблон Агудалина А), поэтому выбрал следующую готовую задачу `P2 №16` (инструкция по развертыванию и запрос ТЗ).
- **Результат:** следующий целевой шаг — реализовать `P2 №16` с критериями воспроизводимого деплоя и явного чек-листа/запроса на ТЗ.

### Шаг 48
- **Задача:** Выполнить `P2 №16` (инструкция по развертыванию и запрос ТЗ).
- **Время:** 2026-03-31 17:55 +03:00
- **Кто исполнил:** devops + orchestrator
- **Как исполнил:** добавил документ `ProjectTP/deployment_and_tz_request.md` с минимальной воспроизводимой инструкцией деплоя (Docker Compose и локальный режим), перечнем эксплуатационных проверок и шаблоном запроса формального ТЗ; синхронизировал статус задачи в `TASKS.md`.
- **Результат:** критерий `P2 №16` закрыт: готова практическая инструкция развертывания и готовый чек-лист/шаблон запроса ТЗ.

### Шаг 49
- **Задача:** Зафиксировать следующий приоритет после закрытия `P2 №16`.
- **Время:** 2026-03-31 17:55 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** проверил `TASKS.md`: `P2 №13` остается приоритетным, но зависит от внешнего входа (шаблон Агудалина А), поэтому следующий доступный к самостоятельному выполнению пункт — `P3 №9`.
- **Результат:** ожидаемый следующий шаг: либо получить шаблон для запуска `P2 №13`, либо выполнить `P3 №9` как неблокирующее улучшение.


### Шаг 50
- **Задача:** Выполнить обязательный предстартовый обзор проекта в новом чате.
- **Время:** 2026-03-31 18:23 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запустил отдельного обзорного агента по всему репозиторию; сверил назначение проекта, стек, ключевые модули и текущее состояние по `HANDOFF.md`/`TASKS.md`.
- **Результат:** подтверждено текущее состояние: закрыты `P2 №10/12/16`, следующий доступный без внешних зависимостей пункт — `P3 №9`.

### Шаг 51
- **Задача:** Выполнить `P3 №9` (позиция `h1` на странице входа).
- **Время:** 2026-03-31 18:23 +03:00
- **Кто исполнил:** frontend
- **Как исполнил:** проверил `frontend/login.html` (DOM уже корректен: `h1` над `loginForm`), исправил только CSS в `frontend/styles.css`: для `.auth-page` добавил `flex-direction: column` и `gap: 16px`, чтобы заголовок отображался над формой визуально.
- **Результат:** критерий `P3 №9` выполнен; статус задачи отмечен как `Выполнено` в `TASKS.md`.

### Шаг 52
- **Задача:** Зафиксировать переход к следующему приоритету после `P3 №9`.
- **Время:** 2026-03-31 18:23 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** после обновления `TASKS.md` проверил оставшиеся пункты; `P2 №13` остается приоритетным, но зависим от внешнего входа (шаблон Агудалина А).
- **Результат:** следующий шаг — ожидание шаблона для запуска `P2 №13`; автономных задач без внешних зависимостей в текущем бэклоге не осталось.

### Шаг 53
- **Задача:** Синхронизировать статусы закрытых задач в `TASKS.md`.
- **Время:** 2026-03-31 18:29 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** сверил `TASKS.md` с фактическим выполнением по `HANDOFF.md` и проставил недостающие отметки `Статус: Выполнено (2026-03-31)` для закрытых задач (`P1 №1/2/3/4/8/11/14/15`, `P2 №5/6/7`).
- **Результат:** `TASKS.md` приведен в соответствие с журналом выполнения; единственная незакрытая задача в бэклоге — `P2 №13` (ожидание внешнего шаблона).

### Шаг 54
- **Задача:** Зафиксировать путь к артефакту `P2 №10` и правило ссылок на новые файлы.
- **Время:** 2026-03-31 18:32 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** по запросу пользователя указал: документ по задаче `P2 №10` — `ProjectTP/push_unblock_checklist.md` (создание и содержание уже отражены в `Шаг 44`); в `TASKS.md` у пункта `№10` добавлена строка **Артефакт** с тем же относительным путём.
- **Результат:** путь к чек-листу виден в `TASKS.md` и дублирует запись в логе `Шаг 44`; при появлении новых файлов по задачам — дописывать в конец `HANDOFF.md` путь и имя файла.

### Шаг 55
- **Задача:** Зафиксировать в политике требование указывать пути к новым файлам в шагах `HANDOFF.md` и свести реестр артефактов из лога.
- **Время:** 2026-03-31 18:36 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** обновил `ProjectTP/subagent-orchestration-policy.md` (раздел 5.3: при создании файла — относительный путь от корня репозитория в том же шаге); обновил `CONTINUE_PROMPT.md` (ссылка на 5.3); в `TASKS.md` у `P2 №16` добавлена строка **Артефакт** с путём к инструкции деплоя; перепроверил лог: **файлы, которые есть в репозитории:** `CONTINUE_PROMPT.md` (корень, шаг 18), `ProjectTP/push_unblock_checklist.md` (шаг 44), `ProjectTP/deployment_and_tz_request.md` (шаг 48). **В логе упомянуты без полного пути, в текущей копии не найдены** (ожидаемое расположение по смыслу задач — `ProjectTP/`): `duty_generation_fairness.md` (шаг 27), `security_audit_passwords_excel.md` (шаг 32), `n8n_notification_workflow.md` (шаг 36) — при необходимости восстановить из истории или пересоздать.
- **Результат:** правила отчёта в `HANDOFF` дополнены; пути к существующим артефактам зафиксированы; для отсутствующих файлов указаны ожидаемые имена и папка.

### Шаг 56
- **Задача:** Дописать относительные пути к файлам прямо в текст существующих шагов лога (по запросу пользователя).
- **Время:** 2026-03-31 18:40 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** уточнил в шагах **18** и **19** пути `ProjectTP/subagent-orchestration-policy.md` и `CONTINUE_PROMPT.md` (корень репозитория); в **шаге 27** — `ProjectTP/duty_generation_fairness.md`; в **шаге 28** — `ProjectTP/subagent-orchestration-policy.md`; в **шаге 32** — `ProjectTP/security_audit_passwords_excel.md`, `ProjectTP/check_excel_flow.py`; в **шаге 36** — `ProjectTP/n8n_notification_workflow.md`, `ProjectTP/docker-compose.yml` (смысл записей не менялся, только дополнены пути).
- **Результат:** артефакты в перечисленных шагах ссылаются на полные относительные пути от корня репозитория.

### Шаг 57
- **Задача:** Восстановить в репозитории отчёт `P1 №15` (файл отсутствовал в git).
- **Время:** 2026-03-31 18:46 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** создан файл `ProjectTP/security_audit_passwords_excel.md` с выводами аудита по текущему `backend/app/main.py` (bcrypt/passlib, каталог Excel, права на `/exports`); в `TASKS.md` у задачи `№15` добавлена строка **Артефакт** с этим путём.
- **Результат:** критерий «задокументировано» для `P1 №15` закреплён отдельным файлом в репозитории; путь: `ProjectTP/security_audit_passwords_excel.md`.

### Шаг 58
- **Задача:** Синхронизировать формулировки задач с фактом (Excel только admin) и восстановить артефакты `P1 №14` и `P2 №6`.
- **Время:** 2026-03-31 19:26 +03:00
- **Кто исполнил:** orchestrator + backend
- **Как исполнил:** в `TASKS.md` обновлены описание и критерий задачи `P1 №4` (скачивание Excel с сервера — **admin**; support — заполнение/финализация); для `P1 №14` добавлены **Артефакт** и файлы `ProjectTP/duty_generation_fairness.md`, `ProjectTP/check_generation_fairness.py`; для `P2 №6` — **Артефакт** `ProjectTP/n8n_notification_workflow.md` и уточнён критерий (webhook + настройка n8n по инструкции); проверен синтаксис `check_generation_fairness.py` (`py_compile`).
- **Результат:** формулировки задач согласованы с API; недостающие документы и smoke для справедливости генерации и n8n добавлены в репозиторий.

### Шаг 59
- **Задача:** Предстартовый обзор проекта и закрытие `P2 №13` (конструктор инструкции по авторизации в инструментах).
- **Время:** 2026-03-31 20:05 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запущен обзорный агент по репозиторию (стек, модули, бэклог); реализованы `GET /api/instruction-tools/template` и `POST /api/instruction-tools/generate`, файл шаблона `ProjectTP/backend/app/instruction_tools_template.json`, UI на вкладке «Настройки», стили в `ProjectTP/frontend/styles.css`, smoke `ProjectTP/check_instruction_tools.py`; статус задачи отмечен в `TASKS.md`.
- **Результат:** пользователь support/admin выбирает инструменты и получает собранный текст; финальные формулировки подставляются заменой JSON после получения шаблона от Агудалина А; smoke на порту 8001: `template: 200 tools 4`, `generate: 200`.

### Шаг 60
- **Задача:** Переделать `P2 №13` под сценарий «Выход сотрудника» с подстановкой ФИО/логина/пароля по утверждённому тексту инструкции.
- **Время:** 2026-03-31 21:00 +03:00
- **Кто исполнил:** backend + frontend
- **Как исполнил:** удалены `GET/POST /api/instruction-tools/*` и `ProjectTP/backend/app/instruction_tools_template.json`; добавлены `build_employee_exit_instruction` и `POST /api/employee-exit/instruction` в `ProjectTP/backend/app/main.py`; в `ProjectTP/frontend/index.html` — кнопка «Выход сотрудника» в шапке и модальное окно с полями; обновлены `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`, `ProjectTP/check_instruction_tools.py`, `TASKS.md`.
- **Результат:** сотрудник support/admin открывает модалку, вводит данные, получает полный текст письма; копирование в буфер из модального окна.

### Шаг 61
- **Задача:** Устранить 404 при формировании инструкции и добавить скачивание Word (.docx).
- **Время:** 2026-03-31 21:20 +03:00
- **Кто исполнил:** backend + frontend + devops
- **Как исполнил:** основной путь API переведён на `/api/employee_exit/instruction` (подчёркивания), дублирован маршрут с дефисом; добавлены `POST .../instruction/docx` и генерация через `python-docx` в `ProjectTP/backend/app/main.py`, зависимость в `ProjectTP/backend/requirements.txt`; в `ProjectTP/frontend/app.js` — кнопка «Скачать Word (.docx)» и `fetch` с `blob`; в `ProjectTP/nginx/default.conf` — `location ^~ /api/`; обновлён `ProjectTP/check_instruction_tools.py`.
- **Результат:** JSON и DOCX отдаются по новым путям; после пересборки образа backend нужен `docker compose build backend` из-за новой зависимости.

### Шаг 62
- **Задача:** Устойчивость к 404 «Not Found» при формировании инструкции и DOCX.
- **Время:** 2026-03-31 21:45 +03:00
- **Кто исполнил:** backend + frontend
- **Как исполнил:** основные пути заменены на короткие `/api/ee_instruction` и `/api/ee_instruction/docx` (старые пути оставлены как алиасы); добавлен публичный `GET /api/ee_instruction/ping` и `/api/employee_exit/ping`; `FastAPI(redirect_slashes=False)`; импорт `python-docx` через try/except с 503 при отсутствии пакета; в `frontend/app.js` — `apiUrl()` для префикса из meta `api-base`, все `apiFetchJson("/api/...")` получают префикс, безопасный разбор не-JSON ответов; в `index.html` — комментарий про подпапку; `check_instruction_tools.py` обновлён.
- **Результат:** при деплое в подпапку можно задать `<meta name="api-base" content="/prefix" />`; для проверки маршрута без сессии: `GET /api/ee_instruction/ping` → `{"ok":true}`.

### Шаг 63
- **Задача:** Перезапуск локальных процессов разработки (без Docker) из-за устаревшего backend на порту 8000 и 404 на `/api/ee_instruction`.
- **Время:** 2026-03-31 21:30 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** остановлены слушатели портов 8000/8080/8001/8002/8010; заново запущены `uvicorn app.main:app` в `ProjectTP/backend` и `ProjectTP/local_dev_proxy.py`; проверка `GET /api/ee_instruction/ping` на 8000 и через proxy 8080 — `200`, JSON `ok`.
- **Результат:** для UI с API нужно открывать `http://127.0.0.1:8080/index.html` (proxy), а не сырой порт 8000 без статики.

### Шаг 64
- **Задача:** Добавить в инструкцию выхода сотрудника блок про домен и поле ввода домена в UI.
- **Время:** 2026-03-31 22:10 +03:00
- **Кто исполнил:** backend + frontend
- **Как исполнил:** в `EmployeeExitInstructionRequest` добавлено поле `domain`; в `build_employee_exit_instruction` после строки про логин/пароль добавлены строки «Ваш домен — …» и «Пример учётной записи в формате домена: домен\логин»; строка про ZOOM использует тот же пример; в `frontend/index.html` и `frontend/app.js` — поле «Домен» и передача в API; обновлён `ProjectTP/check_instruction_tools.py`.
- **Результат:** сотрудник вводит домен (например `rz`); в тексте и DOCX подставляются домен и пример `домен\логин`.

### Шаг 65
- **Задача:** В тексте инструкции явно выводить пример учётной записи как `{domain}\{login}` в шаблоне строки.
- **Время:** 2026-03-31 22:25 +03:00
- **Кто исполнил:** backend
- **Как исполнил:** в `ProjectTP/backend/app/main.py` в `build_employee_exit_instruction` строка «Пример учётной записи…» переведена на `f"... {domain}\\{login}..."` вместо отдельной переменной; та же подстановка в абзаце про ZOOM; удалена промежуточная переменная `domain_login_example`.
- **Результат:** в сгенерированном тексте формат домена и логина задаётся напрямую в f-строке.

### Шаг 66
- **Задача:** Перезапуск локальной связки разработки (uvicorn + `local_dev_proxy`).
- **Время:** 2026-03-31 22:40 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** остановлены процессы на портах 8000 и 8080; заново запущены `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000` из `ProjectTP/backend` с переменными из `.env` (bootstrap admin, `SESSION_SECRET`); запущен `ProjectTP/local_dev_proxy.py` (8080 → 8000 для `/api` и `/exports`).
- **Результат:** UI: `http://127.0.0.1:8080/`; проверка `GET /api/health` и `GET /api/ee_instruction/ping` через proxy — OK.

### Примечание (процесс)
- Ранее не все мелкие правки сопровождались отдельной строкой в логе — дальше при значимых изменениях кода/контракта UI дописывать шаг в **Лог шагов** сразу в том же цикле, без откладывания.

### Шаг 67
- **Задача:** Предстартовый обзор проекта (CONTINUE_PROMPT) и создание новой задачи в бэклоге.
- **Время:** 2026-03-31 23:20 +03:00
- **Кто исполнил:** orchestrator + explore subagent
- **Как исполнил:** запущен обзор репозитория «ProjectTP»: веб-приложение дежурств/отчётов/Excel и инструкции выхода сотрудника; стек FastAPI+SQLite+статический фронт, Docker Compose+n8n опционально; ключевой файл — `ProjectTP/backend/app/main.py`; отмечены пробелы: монолит, без миграций, слабый README, нет полноценного CI. В `TASKS.md` в раздел **Бэклог** добавлена задача **№17** (Alembic + разбиение `main.py`, P2, статус «В очереди»).
- **Результат:** зафиксировано текущее состояние стека и модулей; следующая приоритетная работа для сабагентов — `TASKS.md`, задача **№17** (путь к списку задач: `TASKS.md` в корне репозитория).

### Шаг 68
- **Задача:** Устранить нарушение правил `HANDOFF.md` и технические долги (импорт `Request`, артефакты `.pyc`).
- **Время:** 2026-03-31 23:55 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** блоки **Текущий статус задачи** и **Следующий шаг** возвращены к состоянию до ошибочной перезаписи (соответствие `CONTINUE_PROMPT.md`: дальше — только новые шаги в логе); в правилах обновления убрана отсылка к правке этих блоков вместо лога; в `ProjectTP/backend/app/main.py` для n8n webhook используется `urllib.request.Request` под именем `UrlRequest`, чтобы не затенять `Request` из FastAPI; в корне репозитория добавлен `.gitignore` (`__pycache__/`, `*.py[cod]`), отслеживаемые `.pyc` удалены из индекса git.
- **Результат:** актуальное состояние бэклога и закрытых задач — в `TASKS.md` и в **Лог шагов**; новый файл: `.gitignore` (корень репозитория).

### Шаг 69
- **Задача:** Повторная проверка и доведение исправлений (HANDOFF, n8n `UrlRequest`).
- **Время:** 2026-04-01 12:00 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** блоки **Текущий статус задачи** и **Следующий шаг** снова приведены к зафиксированному «замороженному» виду (до ошибочной подмены на TASK №7 / P2 №10); в `ProjectTP/backend/app/main.py` вызов HTTP-клиента для webhook исправлен с `Request(` на `UrlRequest(` при уже существующем импорте `Request as UrlRequest`.
- **Результат:** затенение `urllib.request.Request` устранено на уровне вызова; актуальный бэклог по-прежнему в `TASKS.md` и в логе шагов.

### Шаг 70
- **Задача:** Реализовать бэклог `TASKS.md` №17 — PostgreSQL, Alembic, отказ от SQLite-обходов при старте.
- **Время:** 2026-04-01 10:20 +03:00
- **Кто исполнил:** orchestrator + backend + devops
- **Как исполнил:** добавлены `DATABASE_URL` и `get_database_url()` в `ProjectTP/backend/app/main.py`, `run_migrations()` с `alembic upgrade head` и `stamp head` для старых SQLite без `alembic_version`; удалены `init_db`/`ensure_*` (PRAGMA/sqlite_master); добавлены `ProjectTP/backend/alembic/`, ревизия `ProjectTP/backend/alembic/versions/a9ed29c6b953_initial_schema.py`, зависимости в `ProjectTP/backend/requirements.txt`; в `ProjectTP/backend/Dockerfile` копируются `alembic` и `alembic.ini`; в `ProjectTP/docker-compose.yml` — сервис `db` (Postgres), том `pgdata`, `DATABASE_URL` по умолчанию; обновлены `TASKS.md`, `ProjectTP/deployment_and_tz_request.md`.
- **Результат:** схема БД версионируется Alembic; в Docker по умолчанию используется PostgreSQL; локально без `DATABASE_URL` остаётся SQLite; задача №17 отмечена выполненной; следующий приоритет в бэклоге — задача №18 (разбиение `main.py`).

### Шаг 71
- **Задача:** Убрать SQLite из основного backend — только PostgreSQL через `DATABASE_URL`.
- **Время:** 2026-04-01 12:30 +03:00
- **Кто исполнил:** orchestrator + backend + devops
- **Как исполнил:** в `ProjectTP/backend/app/main.py` удалены `DATABASE_PATH` и fallback на SQLite; `get_database_url()` требует `postgresql://`/`postgresql+psycopg2://`; `get_engine`/`run_migrations` только под Postgres; в `ProjectTP/backend/alembic/env.py` убрана ветка SQLite; в `ProjectTP/docker-compose.yml` — `DATABASE_URL` для `backend` собирается на хост `db`, порт `5432` проброшен на хост, удалён volume `./data`; в `ProjectTP/.env` добавлен пример `DATABASE_URL` на `127.0.0.1` для локального uvicorn; обновлены `ProjectTP/deployment_and_tz_request.md`, корневой `.gitignore`, `TASKS.md` (формулировка №17).
- **Результат:** приложение без SQLite в основном приложении; локальная разработка — Postgres (например `docker compose up -d db` + uvicorn с `DATABASE_URL` на localhost). Папка `ProjectTP/duty_app` не менялась (отдельный контур).

### Шаг 72
- **Задача:** Начать задачу `TASKS.md` №18 (разбиение монолитного `main.py`) без изменения API-контрактов.
- **Время:** 2026-04-01 12:50 +03:00
- **Кто исполнил:** orchestrator + backend
- **Как исполнил:** вынесены настройки в `ProjectTP/backend/app/config.py`, модели SQLAlchemy в `ProjectTP/backend/app/models.py`, подключение/сессия/миграции в `ProjectTP/backend/app/database.py`; в `ProjectTP/backend/app/main.py` удалены дубли определений и подключены импорты из новых модулей; в `ProjectTP/backend/alembic/env.py` источником `Base` стал `app.models`, а URL — `app.database.get_database_url`.
- **Результат:** `main.py` сокращён на слой конфигурации/ORM/DB, поведение API сохранено; проверка прошла (`python -m py_compile`, импорт `app.main`, smoke `ProjectTP/check_admin_ops.py` — PASS).

### Шаг 73
- **Задача:** Продолжить задачу `TASKS.md` №18 — вынести Pydantic-схемы из `main.py`.
- **Время:** 2026-04-01 13:05 +03:00
- **Кто исполнил:** backend
- **Как исполнил:** создан модуль `ProjectTP/backend/app/schemas.py` со всеми request/response моделями (`LoginRequest`, `DailyReportOut`, `DutySwapOut` и др.); в `ProjectTP/backend/app/main.py` удалены inline-классы `BaseModel` и добавлены импорты из `app.schemas`.
- **Результат:** `main.py` дополнительно сокращён без изменения контрактов API; проверки `py_compile` и smoke `ProjectTP/check_admin_ops.py` — PASS.

### Шаг 74
- **Задача:** Продолжить задачу `TASKS.md` №18 — вынести service/utility блок из `main.py`.
- **Время:** 2026-04-01 13:15 +03:00
- **Кто исполнил:** backend
- **Как исполнил:** создан модуль `ProjectTP/backend/app/services.py` и перенесены функции генерации инструкции/Word (`build_employee_exit_instruction*`), Excel (`build_report_excel`), export-helpers (`exports_dir`, `safe_export_name_part`, `surname_from_full_name`) и `report_to_out`; в `ProjectTP/backend/app/main.py` удалены перенесённые реализации и подключены импорты из `app.services`; в endpoint DOCX вынесено формирование `Content-Disposition` через `attachment_content_disposition_docx`.
- **Результат:** `main.py` дополнительно упрощён, API-контракты сохранены; проверки `py_compile` и smoke `ProjectTP/check_admin_ops.py` — PASS.

### Шаг 75
- **Задача:** Завершить `TASKS.md` №18 — роутеры FastAPI и разделение слоёв.
- **Время:** 2026-04-01 13:35 +03:00
- **Кто исполнил:** orchestrator + backend + qa
- **Как исполнил:** добавлены `ProjectTP/backend/app/security.py`, `ProjectTP/backend/app/deps.py`, `ProjectTP/backend/app/duty_slots.py`; в `ProjectTP/backend/app/services.py` — `EXPORT_FILENAME_RE`, `is_safe_export_filename`; каталог `ProjectTP/backend/app/routers/` (`auth.py`, `instruction.py`, `admin_users.py`, `duties.py`, `duty_swaps.py`, `reports.py`, `exports.py`, `__init__.py` с `register_routers`); `ProjectTP/backend/app/main.py` сведён к созданию приложения, middleware, startup и `register_routers(app)`; исправлено отсутствие импорта `db_session` в startup; прогон smoke: `check_admin_ops.py`, `check_login.py`, `check_instruction_tools.py`; обновлён `TASKS.md` (№18 — выполнено).
- **Результат:** задача №18 закрыта; точка входа компактная, маршруты сгруппированы по модулям; смоки по ключевым API — PASS.

### Шаг 76
- **Задача:** Перезапуск локальной связки разработки после рефакторинга.
- **Время:** 2026-04-01 13:40 +03:00
- **Кто исполнил:** devops
- **Как исполнил:** освобождены порты 8000/8080; попытка `docker compose up -d db` — `docker` не найден в PATH; запуск `uvicorn` — падение на `run_migrations()` с `psycopg2.OperationalError: connection refused` к `127.0.0.1:5432` (сервер БД на хосте не слушает порт).
- **Результат:** код и смоки до перезапуска — в порядке; для рабочего старта нужен доступный PostgreSQL по `DATABASE_URL` из `ProjectTP/.env` (например поднять Postgres через Docker Desktop / установленный сервис), затем снова `uvicorn` и `local_dev_proxy.py`.

### Шаг 77
- **Задача:** Диагностика 502 при логине через `local_dev_proxy` и улучшение ответа прокси.
- **Время:** 2026-04-01 14:10 +03:00
- **Кто исполнил:** orchestrator + backend
- **Как исполнил:** зафиксировано: 502 при `URLError` в `ProjectTP/local_dev_proxy.py` означает недоступность backend `127.0.0.1:8000` (часто из-за незапущенного uvicorn или отказа старта из-за PostgreSQL); для путей `/api/*` вместо HTML-страницы ошибки возвращается JSON с пояснением.
- **Результат:** пользователю проще отличить «backend/БД не подняты» от ошибки логина/пароля (которая даёт 401 уже от API).

### Шаг 78
- **Задача:** Разнести политику оркестрации: политика vs runbook vs конвенции HANDOFF vs примеры.
- **Время:** 2026-04-01 (сессия)
- **Кто исполнил:** orchestrator
- **Как исполнил:** сокращён `ProjectTP/subagent-orchestration-policy.md` (ссылки на вынесенное); добавлены `AGENT_WORKFLOW.md`, `docs/handoff-conventions.md`, `docs/examples-orchestration.md`; обновлены `CONTINUE_PROMPT.md`, `HANDOFF.md` (шапка), `TASKS.md` (конвенция **Артефакт:**), `ProjectTP/agents/README.md` и спецификации techdoc-субагентов.
- **Результат:** старт чата и формат лога документированы отдельно от абстрактной политики оркестрации.

### Шаг 79
- **Задача:** Улучшить старт агента: чеклист, проверка контекста, якорь ACTIVE, правила микрошагов.
- **Время:** 2026-04-01
- **Кто исполнил:** orchestrator
- **Как исполнил:** добавлены `START_CHECKLIST.md`, `scripts/check_agent_context.py`; в `HANDOFF.md` — блок **Якорь ACTIVE**; согласованы `CONTINUE_PROMPT.md`, `AGENT_WORKFLOW.md`, `docs/handoff-conventions.md`, `ProjectTP/subagent-orchestration-policy.md` (логический шаг vs микрошаги); правило создания `HANDOFF.md` при отсутствии файла.
- **Результат:** единый чеклист и опциональная самопроверка; меньше неоднозначности по логированию; ACTIVE для быстрого фокуса.

### Шаг 80
- **Задача:** Продолжение по `CONTINUE_PROMPT.md`: облегчённый старт (шаги 76–79 в логе уже описывают рефакторинг и прокси; полный обзор репозитория не дублировал).
- **Время:** 2026-04-02 12:00 +03:00
- **Кто исполнил:** orchestrator
- **Как исполнил:** прочитал `HANDOFF.md`, `TASKS.md`, `ProjectTP/subagent-orchestration-policy.md`, `AGENT_WORKFLOW.md`, `docs/handoff-conventions.md`; выполнил `python scripts/check_agent_context.py` (OK); проверил среду: `docker` и `node` отсутствуют в PATH (как в шаге 76), `psql` не найден; `python -m py_compile` для всех `ProjectTP/backend/app/**/*.py` — без ошибок.
- **Результат:** кодовая база backend синтаксически валидна; для интеграционных смоков и UI нужен PostgreSQL и при желании Node для `node --check`; критерий **Следующий шаг** (браузер, «Админ → Сотрудники») остаётся на стороне пользователя после поднятия БД и `uvicorn` + `local_dev_proxy.py` по `ProjectTP/deployment_and_tz_request.md`.

### Шаг 81
- **Задача:** Удалить устаревший контур SQLite (`duty_app/`) из репозитория после проверки отсутствия ссылок в скриптах.
- **Время:** 2026-04-08
- **Кто исполнил:** orchestrator
- **Как исполнил:** удалён каталог `ProjectTP/duty_app/`; убрана строка `duty_app/` из `ProjectTP/.dockerignore`; комментарий в `ProjectTP/scripts/setup-docker-rocky.sh` про дубликат скрипта; обновлены `ТЗ-по-проекту.md`, `Проектная-документация.md`, `ProjectTP/security_audit_passwords_excel.md`, `ProjectTP/deployment_and_tz_request.md` (раздел миграции — только для архивных файлов SQLite вне репо).
- **Результат:** в репозитории нет параллельного стека на SQLite; целевая СУБД — PostgreSQL в основном `backend/`.

### Шаг 82
- **Задача:** Продолжение после восстановления `TASKS.md`/`HANDOFF.md`: зафиксировать первоочередные операционные задачи.
- **Время:** 2026-04-08
- **Кто исполнил:** orchestrator
- **Как исполнил:** в `TASKS.md` в раздел **P1** добавлены задачи **№19** (Docker Engine + Compose вместо Podman как целевой рантайм), **№20** (диагностика тома `pgdata`, причины «пустой БД», бэкапы), **№21** (безопасная пересборка и перенос сервера через `pg_dump`); обновлена строка **ACTIVE** в `HANDOFF.md`.
- **Результат:** исполнители берут работу сверху раздела P1; документацию по №19–21 предстоит дописать в `ProjectTP/docs/deploy-rocky-linux.md` / `README.md`.

### Шаг 83
- **Задача:** Выполнить P1 №19–21 — документация Docker, тома Postgres, пересборка и перенос.
- **Время:** 2026-04-08
- **Кто исполнил:** orchestrator + devops
- **Как исполнил:** расширены `ProjectTP/docs/deploy-rocky-linux.md` (целевой Docker, §5.1 про том `pgdata` и диагностику, бэкап перед `-v`, §9 без битой ссылки), обновлены `ProjectTP/README.md`, комментарий healthcheck в `ProjectTP/docker-compose.yml`; в `TASKS.md` задачи №19–21 отмечены выполненными.
- **Результат:** критерии №19–21 закрыты текстом в репозитории; полевая проверка на Rocky/прод — по решению команды.

### Шаг 84
- **Задача:** Продолжение по `CONTINUE_PROMPT.md`: облегчённый старт и техпроверка админ-операций (P1 №1).
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** orchestrator
- **Как исполнил:** прочитал `CONTINUE_PROMPT.md`, `HANDOFF.md`, `TASKS.md`, `ProjectTP/subagent-orchestration-policy.md`, `AGENT_WORKFLOW.md`; полный обзор репозитория не дублировал (актуальны шаги 80–83); стек `docker compose` в `ProjectTP/` уже был поднят (nginx на хосте `8080`→80); выполнен `python3 ProjectTP/check_admin_ops.py` — все шаги 200.
- **Результат:** backend happy-path смены пароля / удаления отчётов / удаления пользователя подтверждён; визуальная проверка кнопок в UI остаётся опциональной для команды.

### Шаг 85
- **Задача:** Реализовать P1 №23 — временная выдача и отзыв прав администратора (№22 пропущен).
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** orchestrator + backend + frontend
- **Как исполнил:** добавлены таблица `admin_role_audit` (Alembic `b7e2c0d14f2a`), модель `AdminRoleAudit`, API `POST .../grant-admin`, `POST .../revoke-admin`, `GET /api/admin/role-audit`; `GET /api/admin/users` возвращает всех пользователей (support+admin), порядок для UI; при удалении support-пользователя очищаются связанные строки аудита; фронт: блок «Права администратора», фильтрация support для графика/отчётов/операций по сотрудникам; smoke `ProjectTP/check_admin_delegate.py`; README обновлён; `TASKS.md` №23 — выполнено.
- **Результат:** делегирование admin и журнал назначений работают; последнего админа снять нельзя; support не может вызвать эндпоинты без роли admin.

### Шаг 86
- **Задача:** P1 №23 — UX: права администратора через флажок в таблице пользователей (вместо селекта только по support).
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** в `ProjectTP/frontend/index.html` и `frontend/app.js` одна таблица всех пользователей с колонкой «Права администратора» (чекбокс); убраны отдельный селект/кнопка и вторая таблица админов; обновлён `ProjectTP/README.md`.
- **Результат:** выдача/отзыв прав согласованы с формулировкой «вкладка Админ»; пустой селект при отсутствии чистых support не воспроизводится.

### Шаг 87
- **Задача:** Флажки не видны в UI — кэш статики / видимость чекбоксов.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend + devops
- **Как исполнил:** в `index.html`/`login.html` добавлены query-параметры к `app.js` и `styles.css`; в `styles.css` — явные стили для `#adminUsersEditorTable` чекбоксов; в `nginx/default.conf` — `Cache-Control: no-cache` для `location /`; пустой список пользователей и ошибка загрузки показывают строку-подсказку; `onIndexInit` ловит ошибку `loadEmployees`.
- **Результат:** после пересборки nginx и Ctrl+F5 подтягивается актуальный JS; чекбоксы заметнее.

### Шаг 88
- **Задача:** Запретить снятие прав администратора с изначального (bootstrap) админа.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** backend + frontend
- **Как исполнил:** в `app/deps.py` — `is_bootstrap_admin_account()` по `BOOTSTRAP_ADMIN_USERNAME`; в `POST .../revoke-admin` — 400 для bootstrap; в `UserOut`/`UserMeOut` — поле `is_bootstrap_admin`; UI: флажок заблокирован для bootstrap; `check_admin_delegate.py`, `README.md`, `index.html`, `.env.example`, bump `app.js`/`styles.css` query v5.
- **Результат:** изначального админа нельзя разжаловать ни при нескольких админах, ни как «единственного»; без `BOOTSTRAP_ADMIN_USERNAME` в `.env` защита по логину не срабатывает.

### Шаг 89
- **Задача:** P2 №24 — убрать у support вводящую в заблуждение кнопку сохранения на графике; не сломать админа.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** `body.admin-user` + CSS для `#saveDutiesBtn`; подпись «Сохранить правки графика»; у support автозагрузка при смене `dutiesDate`, кнопка «Обновить» только у админа, подсказка support-only; `TASKS.md` №24 — выполнено; bump статики `?v=20260408-6`.
- **Результат:** support на вкладке «График» не видит сохранение графика; админ — «Обновить» + сохранение правок.

### Шаг 90
- **Задача:** P2 №25 — отчёты для support без выбора сотрудника и без «Показать».
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** кнопка `#loadReportsBtn` — `admin-only`, подпись «Обновить»; CSS `body:not(.admin-user) #loadReportsBtn`; подсказка `support-only`; API уже ограничивает support своими отчётами; автозагрузка — существующие `change` на `#reportsDate` и стартовый блок; `TASKS.md` №25 — выполнено; bump `?v=20260408-7`.
- **Результат:** support видит дату и свои отчёты при смене даты; админ — выбор сотрудника + «Обновить».

### Шаг 91
- **Задача:** P2 №29 — в UI отчётов показывать суммарное время по строкам (часы/минуты).
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** в `app.js` — `formatWorkDurationTotalMinutes`, сумма по полям «Минуты», блок «Всего» под таблицей, пересчёт на `input` и при добавлении/удалении строк; в `styles.css` — отступ для `.report-minutes-total`; bump `?v=20260408-8`; `TASKS.md` №29 — выполнено.
- **Результат:** под каждой карточкой отчёта видна сумма в формате «N ч M мин»; пустые/некорректные значения минут дают 0 в сумме для этой строки.

### Шаг 92
- **Задача:** P2 №28 — Excel и UI отчётов: колонки «Задача», «Результат», «Время работы»; шапка дата/ФИО как у заказчика.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** backend + frontend
- **Как исполнил:** миграция `c1d4e8f0a1b2` — поле `report_entries.task`; API `ReportEntryIn`/`Out`; `build_report_excel` (openpyxl: merge B:C для даты и ФИО, строки данных, `minutes_ru_label`); фронт — три колонки ввода; bump `?v=20260408-9`; `TASKS.md` №28 — выполнено.
- **Результат:** после `alembic upgrade head` старые строки получают пустую задачу; выгрузка .xlsx совпадает с макетом.

### Шаг 93
- **Задача:** P2 №30 — неактивные к дежурствам не в селектах графика; метка админу; проверки API.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** backend + frontend
- **Как исполнил:** `GET /api/duties` — `UserOut.is_active_for_duties`; `POST /api/duties/batch` — запрет назначения неактивного; `POST /api/duty-swaps` — запрос обмена только если участник активен; фронт — `supportEmployeesForDutySlotSelect`, подпись «не в графике дежурств» в таблице сотрудников и в селектах/просмотре графика; `check_inactive_duties.py`; bump `?v=20260408-10`; `TASKS.md` №30 — выполнено.
- **Результат:** в слоте с уже назначенным неактивным он остаётся в списке с пометкой; сохранить график с новым назначением неактивного нельзя.

### Шаг 94
- **Задача:** P3 №32 — подсветка строк графика, где слот назначен на текущего пользователя.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** класс `duty-row-self` + `title`; стили зелёного фона, для пересечения с `current-slot` — акцент полосой; bump `?v=20260408-11`; `TASKS.md` №32 — выполнено.
- **Результат:** свои слоты на выбранную дату визуально отделены; подсказка при наведении.

### Шаг 95
- **Задача:** P2 №26 — инвентаризация уведомлений и правила против дублей (без Битрикс).
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** orchestrator
- **Как исполнил:** добавлен `ProjectTP/docs/notifications-inventory.md` (таблица каналов, n8n/UI, отключённый путь при пустом `N8N_WEBHOOK_URL`, план №22, анти-спам); ссылка в `README.md`; `TASKS.md` №26 — выполнено.
- **Результат:** зафиксировано «что есть сейчас»; перенос в Битрикс остаётся в задаче №22.

### Шаг 96
- **Задача:** P2 №31 — каталог приложения переименован в `ProjectTP`.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** orchestrator + devops
- **Как исполнил:** массовая замена путей `Проект Проектов` → `ProjectTP` в текстах репозитория; `mv` каталога; правка `docs/deploy-rocky-linux.md` (cd в клоне); удалён локальный `backend/.venv`; `TASKS.md` №31 — выполнено.
- **Результат:** приложение в `ProjectTP/`; старый путь в актуальных файлах не используется; для локального Python — `python3 -m venv backend/.venv` заново при необходимости.

### Шаг 97
- **Задача:** Продолжение по `CONTINUE_PROMPT.md`: smoke-тесты и устранение падения `check_swap_request.py` при очистке пользователей после сценария обмена.
- **Время:** 2026-04-08 (сессия)
- **Кто исполнил:** orchestrator + backend
- **Как исполнил:** воспроизведён `500`/`Internal Server Error` при `DELETE` support-пользователя с записями в `duty_swap_requests`; в `ProjectTP/backend/app/routers/admin_users.py` перед удалением пользователя добавлено `delete(DutySwapRequest)` по `requester_user_id`/`target_user_id`; в `ProjectTP/check_swap_request.py` — устойчивый разбор тела ответа (`strip`); пересобран образ backend, перезапущены backend и nginx; прогон `python3 ProjectTP/check_swap_request.py` — OK.
- **Результат:** админ может удалить сотрудника после сценариев обмена; смок обмена с cleanup завершается без ошибки.
- **Статус:** success

### Шаг 98
- **Задача:** Кнопка «Скачать Excel» у отчёта выбранного сотрудника не открывала файл после формирования / при повторном открытии страницы.
- **Время:** 2026-04-09 (сессия)
- **Кто исполнил:** frontend
- **Как исполнил:** причина — после `finalize` вызывался `loadReports()`, карточка пересоздавалась с `href="#"` и скрытой ссылкой; в `ProjectTP/frontend/app.js` для админа при `status === "final"` рендерится видимая ссылка на `/exports/report_{id}_{date}.xlsx` (совпадает с `report_excel_filename` в backend), добавлен `appRootPath` для `api-base`; убрана дублирующая установка ссылки в `finalizeReportExcel`; bump `app.js` в `index.html` до `?v=20260409-1`.
- **Результат:** скачивание Excel для конкретного сотрудника работает после «Сформировать Excel» и при следующей загрузке отчётов.

### Шаг 99
- **Задача:** Интеграция Битрикс24 (чат): вебхук, график на день, уведомление за 2 минуты, привязка ID сотрудника и упоминания в тексте.
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** backend + frontend + qa (агент)
- **Как исполнил:** в `ProjectTP/backend/app/config.py` и `ProjectTP/docker-compose.yml` / `ProjectTP/.env.example` — переменные `BITRIX_INCOMING_WEBHOOK_URL`, `BITRIX_NOTIFY_DIALOG_ID`, таймаут; в `ProjectTP/backend/app/routers/duties.py` — `_bitrix_messaging_pair`, `_bitrix_im_message_add`, расширен `POST /api/admin/notifications/duty-upcoming/dispatch` (n8n + Bitrix), новый `POST /api/admin/notifications/duty-schedule/bitrix`; модуль `ProjectTP/backend/app/bitrix_mention.py` — формат `[USER=id]…[/USER]` для строк графика и текста «через 2 минуты»; скрипты `ProjectTP/check_bitrix_message.py`, `ProjectTP/backend/send_schedule_bitrix.py`; тесты `ProjectTP/backend/requirements-dev.txt`, `ProjectTP/backend/pytest.ini`, `ProjectTP/backend/tests/test_bitrix_webhook.py`; в `ProjectTP/frontend/index.html` и `ProjectTP/frontend/app.js` — карточка «Уведомления», кнопка «Уведомить о дежурствах», колонка «Битрикс» в таблице сотрудников, `PATCH /api/admin/users/{id}/bitrix-user` при сохранении строки, опциональное поле при создании пользователя.
- **Результат:** админ может отправить график на сегодня в настроенный чат; при наличии `N8N_WEBHOOK_URL` и/или Bitrix работают оба канала; для проверки вебхука — `python3 ProjectTP/check_bitrix_message.py` из каталога `ProjectTP/` с заполненным `.env`.

### Шаг 100
- **Задача:** Поле `bitrix_user_id` у пользователей (Битрикс) и скрытие его в публичных ответах API графика/отчётов.
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** миграция `ProjectTP/backend/alembic/versions/e4f5a6b7c8d9_users_bitrix_user_id.py`; колонка и поле в `ProjectTP/backend/app/models.py`; в `ProjectTP/backend/app/schemas.py` — `UserOut.bitrix_user_id`, `AdminBitrixUserIdRequest`, опционально в `CreateSupportUserRequest`; `ProjectTP/backend/app/routers/admin_users.py` — `PATCH /api/admin/users/{user_id}/bitrix-user`, выдача ID в `_user_out`, создание с `bitrix_user_id`; `ProjectTP/backend/app/routers/auth.py` — `UserMeOut` с полем; в `ProjectTP/backend/app/routers/duties.py` и `ProjectTP/backend/app/services.py` при сборке `UserOut` для графика и отчётов — `bitrix_user_id=None` (не светить коллегам); тест `ProjectTP/backend/tests/test_bitrix_mention.py`.
- **Результат:** ID хранится в БД и редактируется в админке; в чат уходит график с BB-упоминаниями для заполненного ID; в `GET /api/duties` и сотруднике в отчёте поле не отдаётся.

### Шаг 101
- **Задача:** Зафиксировать эксплуатационное правило Битрикс: куда слать график (`BITRIX_NOTIFY_DIALOG_ID`) vs `bitrix_user_id` для упоминаний; служебный пользователь `user` / чат `chat441509`.
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `ProjectTP/docs/notifications-inventory.md` — уточнение в строке про Битрикс24: `DIALOG_ID` для `im.message.add` задаётся отдельно от поля «Битрикс» в карточке сотрудника; групповой чат — префикс `chatNNN`; пример: `bitrix_user_id=441509` у учётки `user` соответствует диалогу **`chat441509`**, голый `441509` для этого канала даёт ошибку API. В `ProjectTP/check_bitrix_message.py` восстановлен `import sys` (используется в сообщениях об ошибках).
- **Результат:** меньше путаницы «отправить user / в чат общий» vs личка конкретного сотрудника; смок-скрипт снова импортирует `sys` корректно.

### Шаг 102
- **Задача:** Реализовать `TASKS.md` №32: уведомления о предстоящем дежурстве (ЛС за 5 минут, ЛС в момент старта, дублирование в общий чат в момент старта).
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** backend + orchestrator
- **Как исполнил:** в `ProjectTP/backend/app/routers/duties.py` расширен `POST /api/admin/notifications/duty-upcoming/dispatch` параметром `mode=upcoming_2m|upcoming_5m|start`, добавлены alias-эндпоинты `POST /api/admin/notifications/duty-upcoming/5m` и `POST /api/admin/notifications/duty-upcoming/start`; для `upcoming_5m` отправляется ЛС сотруднику по `bitrix_user_id`, для `start` — ЛС сотруднику + сообщение в `BITRIX_NOTIFY_DIALOG_ID`; добавлена защита от ложных срабатываний вне минуты триггера (`HH:55` для `5m`, `HH:00` для `start`). В `ProjectTP/backend/app/schemas.py` расширен `DutyNotificationDispatchOut`; синхронизированы `TASKS.md` и `ProjectTP/docs/notifications-inventory.md`; добавлены unit-тесты helper-логики в `ProjectTP/backend/tests/test_bitrix_webhook.py`.
- **Результат:** критерий №32 закрыт: уведомление за 5 минут и в момент старта уходит в ЛС, а в момент старта также дублируется в общий чат; ответ API теперь показывает отдельно статус персональной и чат-отправки.

### Шаг 103
- **Задача:** Добавить управление способом запуска уведомлений (`cron`/`n8n`) и флажками отправки из админ-панели.
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** backend + frontend + orchestrator
- **Как исполнил:** в `ProjectTP/backend/app/models.py` добавлена модель `DutyNotificationSettings` и миграция `ProjectTP/backend/alembic/versions/f1a2b3c4d5e6_duty_notification_settings.py`; в `ProjectTP/backend/app/schemas.py` добавлены `DutyNotificationSettings*`; в `ProjectTP/backend/app/routers/duties.py` добавлены `GET/PATCH /api/admin/notifications/settings`, параметр `source=cron|n8n` в dispatch и применение флажков per-method (`5m`, `start`, `chat_on_start`). Во `ProjectTP/frontend/index.html` и `ProjectTP/frontend/app.js` в секции «Админ → Уведомления» добавлены чекбоксы выбора активного способа запуска и чекбоксы управления отправкой по каждому способу.
- **Результат:** админ может включать/отключать уведомления флажками и переключать активный способ запуска; backend отправляет уведомления только для выбранного способа и включённых флагов.

### Шаг 104
- **Задача:** Доработать UX управления уведомлениями: переключение по клику, круглые взаимоисключающие переключатели и скрытие неактивного блока настроек.
- **Время:** 2026-04-15 (сессия)
- **Кто исполнил:** frontend + orchestrator
- **Как исполнил:** в `ProjectTP/frontend/index.html` для выбора способа запуска `cron/n8n` чекбоксы заменены на radio-кнопки (`name="notificationMethod"`), удалена кнопка ручного сохранения; блоки настроек разделены на `#notifCronSettingsBlock` и `#notifN8nSettingsBlock`. В `ProjectTP/frontend/app.js` добавлены автосохранение настроек на `change`, управление видимостью только активного блока и статусные сообщения сохранения; в `ProjectTP/frontend/styles.css` добавлены стили: круглые переключатели для метода запуска и квадратные чекбоксы для множественного выбора уведомлений.
- **Результат:** переключение способа и галочек применяется сразу по клику из админ-панели; настройки неактивного способа скрыты.

### Шаг 105
- **Задача:** Соответствие генерации дежурств правилам продукта (bootstrap вне автогенерации, не более 2 слотов на человека в день, справедливый рандом среди допустимых) и мелкие правки UX/handoff-долга.
- **Время:** 2026-04-16 (сессия)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в `ProjectTP/backend/app/routers/duties.py` — константа `MAX_DUTY_SLOTS_PER_USER_PER_DAY`, исключение из пула `POST /api/duties/generate` учётки из `BOOTSTRAP_ADMIN_USERNAME` (`is_bootstrap_admin_account`), при выборе слота приоритет тем, у кого в этот день уже меньше двух назначений (иначе весь пул — вынужденные повторы при нехватке людей на `SLOT_COUNT` слотов), хелпер `_duty_slot_counts_per_user` и проверка лимита при `POST /api/duties/batch`. Обновлены `ProjectTP/duty_generation_fairness.md` и `ProjectTP/check_generation_fairness.py` (пул без bootstrap, проверка max 2 слотов в день по `GET /api/duties`). Во `ProjectTP/frontend/app.js` — `buildAdminUserEditorRow` / `replaceAdminUserEditorRow`, чтобы после «Сохранить» у одного сотрудника не сбрасывались несохранённые правки в других строках таблицы. В `ProjectTP/frontend/index.html` — упрощён поясняющий текст в блоке «Уведомления».
- **Результат:** автогенерация и ручной batch согласованы с лимитом 2 слота/день/человек; изначальный админ в график попадает только вручную; smoke fairness учитывает новые правила; правки сотрудников в таблице не теряются при точечном сохранении.

### Шаг 106
- **Задача:** Зафиксировать эксплуатационную модель «общий чат» в Битрикс24 для уведомлений и графика.
- **Время:** 2026-04-16 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** зафиксировано устное правило заказчика: общий канал — это пользователь портала `user` с ФИО «Все» и числовым **id 441509** в Битрикс; для `im.message.add` в групповой чат в `.env` использовать **`BITRIX_NOTIFY_DIALOG_ID=chat441509`** (префикс `chat`), для ЛС этому же субъекту — **`441509`**; в приложении у учётки `user` в карточке сотрудника указать **`bitrix_user_id=441509`**. Дополнено `ProjectTP/docs/notifications-inventory.md` примером развёртывания (не как обязательный дефолт для всех инсталляций).
- **Результат:** меньше путаницы между личным диалогом и групповым `chatNNN` при настройке вебхука и рассылки при старте дежурства.

### Шаг 107
- **Задача:** Прогон unit-тестов, связанных с уведомлениями Битрикс (вебхук, разбор слота, BBCode имени).
- **Время:** 2026-04-16 (сессия)
- **Кто исполнил:** qa (агент)
- **Как исполнил:** в примерах `tests/test_bitrix_webhook.py` и `tests/test_bitrix_mention.py` для сценария ЛС использован примерный `bitrix_user_id` **11751**; выполнено `docker compose run` по образу `backend` с монтированием каталога `ProjectTP/backend`, установка `pytest`, команда `PYTHONPATH=. python -m pytest tests/ -v` — **9 passed**.
- **Результат:** регрессии по мокам `im.message.add` и `_resolve_notification_slot` не выявлены; интеграция с живым порталом по-прежнему вне автотестов (см. `ProjectTP/check_bitrix_message.py` при заполненном `.env`).

### Шаг 108
- **Задача:** Диагностика «в :55 нет уведомления» — расхождение часового пояса Docker и cron.
- **Время:** 2026-04-16 (сессия)
- **Кто исполнил:** devops + orchestrator (агент)
- **Как исполнил:** в `ProjectTP/backend/Dockerfile` установлен пакет `tzdata`; в `ProjectTP/docker-compose.yml` для backend добавлен проброс **`TZ: ${TZ:-UTC}`**; в `ProjectTP/.env.example` — переменная `TZ` с комментарием; в `ProjectTP/docs/notifications-inventory.md` — раздел про `:55`/`datetime.now()` и необходимость `TZ` (пример МСК); в локальном `ProjectTP/.env` добавлено **`TZ=Europe/Moscow`** для согласования с cron по местному времени.
- **Результат:** после `docker compose up -d --build backend` время в контейнере можно выровнять с хостом/cron; напоминание: вызов `POST .../duty-upcoming/5m?source=cron` с сессией админа по-прежнему обязателен по расписанию.

### Ifu 109
- **Задача:** Довести уведомления о дежурствах (Битрикс ЛС + общий чат), встроенный планировщик, тест из админки, устойчивость Docker и исправления регрессий на проде.
- **Время:** 2026-04-16 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** вынесена логика в `ProjectTP/backend/app/bitrix_notify.py` (в т.ч. нормализация ID чата `2237493` → `chat2237493`), `ProjectTP/backend/app/duty_notifications.py` (dispatch, единые настройки, `POST /api/admin/notifications/duty-test`); `ProjectTP/backend/app/notification_scheduler.py` (APScheduler :55/:00 по `TZ`, ленивый импорт APScheduler); `ProjectTP/main.py` — обёртка старта/остановки планировщика; миграция `ProjectTP/backend/alembic/versions/g8h9i0j1k2l3_duty_notification_scheduler_enabled.py` (`scheduler_enabled`, идемпотентный DDL); `ProjectTP/backend/entrypoint.sh` — проверка непустого `SESSION_SECRET`; `ProjectTP/backend/docker_healthcheck.py` + healthcheck в `ProjectTP/docker-compose.yml` (`python3`, увеличен `start_period`); `ProjectTP/backend/requirements.txt` — `apscheduler<4`, `tzdata`; UI: три чекбокса + планировщик + «Отправить тест» (`ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`). Исправления после прод: в `duty_notifications.py` заменён несуществующий импорт `UrlRequest` на `urllib.request.Request as UrllibRequest`; для теста уведомлений — `db.execute(...).scalars().first()` вместо `.first()` (SQLAlchemy 2.0, устранение 500). Обновлены `ProjectTP/backend/send_schedule_bitrix.py`, `ProjectTP/.env.example`.
- **Результат:** happy-path админки для теста ЛС и автоматической рассылки согласован с кодом; контейнер backend не должен падать на импорте и healthcheck при корректном `.env`.
- **Статус:** success

### Шаг 110
- **Задача:** Обновить правило справедливой генерации: утренний дежурный занимает оба слота `07:00` и `08:00`, без изменения остальных ограничений.
- **Время:** 2026-04-17 16:56 +0300
- **Кто исполнил:** backend (агент)
- **Как исполнил:** в `ProjectTP/backend/app/duty_generation.py` добавлены индексы утренней пары слотов и логика фиксации одного `morning_pair_user_id` на день: назначенный на `07:00` автоматически ставится на `08:00`; сохранены существующие правила (`09:00` фиксирован за логином `user`, лимит `MAX_DUTIES_PER_DAY=2`, действующий отбор кандидатов по равномерности).
- **Результат:** генератор теперь всегда формирует связку `07:00`+`08:00` на одного сотрудника при прежних остальных правилах; синтаксис модуля проверен (`python3 -m py_compile`), по файлу нет lints.
- **Статус:** success

### Шаг 111
- **Задача:** Текст тестового ЛС в Битрикс и упрощение карточки «Уведомления» в админке.
- **Время:** 2026-04-17 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `ProjectTP/backend/app/duty_notifications.py` — сообщения `send_test_duty_notification` без префикса «Тестовое», без даты/ID в тексте, с фразой «Хорошего вам дня. Обнял, приподнял, покружил, поставил.»; во `ProjectTP/frontend/index.html` — удалён поясняющий абзац под заголовком «Уведомления».
- **Результат:** тест из админки ближе к боевому тону; дублирующее описание Bitrix/TZ убрано из UI (детали остаются в `docs/notifications-inventory.md` и `.env.example`).
- **Статус:** success

### Шаг 112
- **Задача:** Реализовать `TASKS.md` P2 **№33** — QR и публичная read-only страница с текстом инструкции «выход сотрудника» (токен + TTL).
- **Время:** 2026-04-24 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** миграция `ProjectTP/backend/alembic/versions/n5o6p7q8r9t0_employee_exit_instruction_shares.py`, модель `EmployeeExitInstructionShare`, `EE_INSTRUCTION_SHARE_TTL_SECONDS` в `ProjectTP/backend/app/config.py`, эндпоинты в `ProjectTP/backend/app/routers/instruction.py` (`POST /api/ee_instruction/share`, `GET /p/ee/{token}`, `GET /api/ee_instruction/qr/{token}`), `build_qrcode_png_bytes` в `ProjectTP/backend/app/services.py`, зависимость `qrcode[pil]`; UI в `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`; прокси `/p/` в `ProjectTP/local_dev_proxy.py` и `ProjectTP/nginx/default.conf`; расширен `ProjectTP/check_instruction_tools.py`; обновлены `TASKS.md`, `ProjectTP/docs/roadmap.md`, `ProjectTP/.env.example`.
- **Результат:** happy-path по смоку после `alembic upgrade` и логина: публичная страница отдаёт тот же текст, PNG — валидный QR; по истечении `expires_at` — 410. Строка **ACTIVE** в `HANDOFF.md` обновлена под следующий фокус бэклога.
- **Статус:** success

### Шаг 113
- **Задача:** Реализовать `TASKS.md` P2 **№34** — обмен дежурствами, когда второй участник с ролью **admin** и стоит в графике.
- **Время:** 2026-04-24 (сессия)
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** карточка `#swapCard` убрана из класса `support-only` (раньше `setAdminMode` скрывал её всем админам); в `setAdminMode` явно `swapCard.hidden = false` для support и admin; убран ранний выход и ограничение таймера опроса входящих в `refreshSwapInbox` для админов. Подпись поля целевого слота уточнена («другого дежурного»). Backend уже принимал цель через `ensure_support_or_admin_user` в `ProjectTP/backend/app/routers/duty_swaps.py`.
- **Результат:** админ с участием в графике видит форму обмена и входящие запросы; может отправить запрос в слот, где назначен другой admin/support; `TASKS.md` №34 отмечен выполненным.
- **Статус:** success

### Шаг 114
- **Задача:** Реализовать `TASKS.md` P2 **№35** и **№36** — автогенерация без субботы/воскресенья; сохранение графика с пустыми слотами (`user_id: null`).
- **Время:** 2026-04-24 (сессия)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в `duty_generation.run_generation` при `weekday()>=5` не создаются новые назначения (при `overwrite=false` сохраняются существующие); `DutiesBatchSlot.user_id` опционален; `duties_live.duties_batch` требует все слоты `0..SLOT_COUNT-1`, удаляет строки при `null`, проверка ≤2 слотов/день/человек через `_merged_duty_slot_counts`; UI: подсказки в `index.html`, `saveDuties` шлёт `null` за «—»; обновлены `duty_generation_fairness.md`, `check_generation_fairness.py` (проверка выходных), `check_swap_request.py` / `check_inactive_duties.py` (полный список слотов).
- **Результат:** автогенерация не заполняет выходные; админ сохраняет частично заполненный день; `TASKS.md` №35–№36 отмечены выполненными.
- **Статус:** success

### Шаг 115
- **Задача:** Выполнить `TASKS.md` P2 **№37** — гигиена репозитория без регрессий по рабочим сценариям.
- **Время:** 2026-04-24 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** удалён неиспользуемый `ProjectTP/backend/app/routers/duties.py` (дубликат маршрутов; в `register_routers` подключён только `duties_live`); удалены `ProjectTP/desktop.ini`, дампы `ProjectTP/backup-2026-04-13.sql`, `ProjectTP/backup-2026-04-16.sql`; в `ProjectTP/docs/notifications-inventory.md` и карточках **№22/№32** в `TASKS.md` путь к роутеру заменён на `duties_live.py`; комментарий в `duty_notify_dispatch.py`; в корневом `.gitignore` — игнор `backup-*.sql` и `desktop.ini`.
- **Результат:** меньше мёртвого кода и случайных артефактов в дереве; smoke/unit не удалялись; `TASKS.md` №37 отмечен выполненным.
- **Статус:** success

### Шаг 116
- **Задача:** Выполнить `TASKS.md` P3 **№38** (современный UI: ориентиры Ant Design + Material Design 3).
- **Время:** 2026-05-04 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `ProjectTP/frontend/styles.css` добавлены дизайн-токены в `:root` (ритм 8px, палитра, радиусы, тени elevation), обновлены поверхности секций/карточек/модалок, типографика и `:focus`/hover для кнопок и полей; страница входа — градиент фона и стили `h1`; в `ProjectTP/frontend/index.html` и `ProjectTP/frontend/login.html` bump параметров `styles.css`/`app.js` (`?v=20260504-m38`); статус задачи в `TASKS.md`.
- **Результат:** критерий №38 закрыт; приостановленная **№27** остаётся вне исполнения; новых обязательных пунктов в бэклоге нет.
- **Статус:** success

### Шаг 117
- **Задача:** Сделать рестайлинг P3 №38 визуально заметным (обратная связь «не увидел изменений»).
- **Время:** 2026-05-04 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** усилены контрасты в `ProjectTP/frontend/styles.css`: фон приложения — вертикальный градиент + более холодный серо-синий тон; шапка — синий градиент, нижняя голубая полоса, маркер у `.brand::before`; вкладки — нижняя синяя черта у активной; блоки `.section` — толстый левый primary-бортик и более выраженная тень; кнопки в `.topbar` — полупрозрачные «стеклянные»; страница входа — насыщенный голубой градиент, акцентная полоса под `h1`, карточка с левой полосой; bump `?v=20260505-visible` в `ProjectTP/frontend/index.html` и `login.html`.
- **Результат:** отличия от прежнего плоского светло-серого UI считываются сразу; при кэше статики — Ctrl+F5 или новый `?v=`.
- **Статус:** success

### Шаг 118
- **Задача:** Плавность нажатий кнопок и перехода между вкладками (меню).
- **Время:** 2026-05-04 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `ProjectTP/frontend/styles.css` добавлены `--motion-medium`, easing `cubic-bezier(0.2, 0, 0, 1)`; у `.btn` и `.tab-btn` — `transform: scale` на `:active`, общие transition; у `.tab-panel.active` — `@keyframes tabPanelEnter` (opacity + лёгкий `translateY`); при `prefers-reduced-motion` длительности и анимация панелей отключены; bump `?v=20260505-motion` в `ProjectTP/frontend/index.html` и `login.html`.
- **Результат:** клики по кнопкам и вкладкам дают тактильный отклик; смена раздела — краткое появление контента без смены логики `app.js`.
- **Статус:** success

### Шаг 119
- **Задача:** Проверка визуальной консистентности и выравнивание токенов в `styles.css`.
- **Время:** 2026-05-04 (сессия)
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `:root` добавлены семантические цвета для `.msg`, `--focus-ring`, `--shadow-topbar`; границы кнопок/секций через токены; тулбар/экшены/списки отчётов — отступы `var(--space-*)`; выровнен padding `.report-card`; типографика `h3` в карточке и модалке, стили `h4` и `hr`; оформление `code`; bump `?v=20260505-consistency` в `ProjectTP/frontend/index.html` и `login.html`.
- **Результат:** меньше разрозненных hex в компонентах, единый ритм; цвета строк графика (свой/текущий слот) намеренно оставлены сигнальными.
- **Статус:** success

### Шаг 120
- **Задача:** Точечная UI-правка списка сотрудников: убрать подпись «не в графике дежурств» и выделять нераспределяемых сотрудников красным.
- **Время:** 2026-05-08 10:16 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** выполнен облегчённый старт под пользовательскую точечную задачу по конкретному экрану (без повторного полного обзора репозитория); в `ProjectTP/frontend/app.js` удалён рендер `admin-duty-inactive-tag` и добавлен класс строки `admin-user-duty-inactive`; в `ProjectTP/frontend/styles.css` добавлены стили красного выделения для полей логина/ФИО у неактивных к дежурствам сотрудников.
- **Результат:** в таблице сотрудников подпись удалена, а пользователи вне графика визуально выделяются красным; линтер по изменённым файлам без ошибок.
- **Статус:** success

### Шаг 121
- **Задача:** Переименовать кнопку открытия модального окна инструкции, чтобы исключить трактовку как «выход из аккаунта».
- **Время:** 2026-05-08 10:23 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` текст кнопки `#employeeExitOpenBtn` изменён с «Выход сотрудника» на «Памятка сотруднику» по выбору пользователя.
- **Результат:** кнопка в шапке однозначно воспринимается как действие по открытию инструкции для сотрудника.
- **Статус:** success

### Шаг 122
- **Задача:** Удалить из бэклога задачу `P2 №27` по запросу пользователя.
- **Время:** 2026-05-08 10:24 +0300
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** из `TASKS.md` удалена карточка `### 27) Инструкция по выходу: доработка — выборки (чекбоксы) инструментов`; в `HANDOFF.md` в строке **ACTIVE** убрана ссылка на `№27`, чтобы не оставалось неактуальных упоминаний.
- **Результат:** задача `№27` полностью исключена из актуального списка задач и из текущего фокуса handoff.
- **Статус:** success

### Шаг 123
- **Задача:** Добавить новые задачи развития сайта в бэклог по согласованию с пользователем.
- **Время:** 2026-05-08 10:50 +0300
- **Кто исполнил:** orchestrator (агент)
- **Как исполнил:** в `TASKS.md` добавлены карточки `№39`–`№43`: вкладка «Полезные ресурсы» (frontend-список ссылок, без backend), календарный вид графика, раздельные права доступа, редактируемые шаблоны уведомлений, мини-аналитика.
- **Результат:** бэклог расширен новыми задачами с приоритетом, критериями готовности и зависимостями; отклонённая ранее идея `№1` в задачи не добавлялась.
- **Статус:** success

### Шаг 124
- **Задача:** Начать выполнение `TASKS.md` №39 — вкладка «Полезные ресурсы».
- **Время:** 2026-05-08 11:05 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` добавлены вкладка/панель `resources` и контейнер `#resourcesGrid`; в `ProjectTP/frontend/app.js` добавлен фронтовый список ресурсов и рендер карточек с изображением (SVG data URI), названием и подписью; ссылки открываются в новой вкладке с `target="_blank"` и `rel="noopener noreferrer"`; в `ProjectTP/frontend/styles.css` добавлены стили сетки и карточек; обновлены query-версии статики.
- **Результат:** MVP вкладки «Полезные ресурсы» работает без backend и готов к наполнению реальными ссылками/картинками.
- **Статус:** success

### Шаг 125
- **Задача:** Добавить ресурс Open WebUI во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:10 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `Open WebUI` со ссылкой `https://i.hpdd.ru/`, подписью и изображением, переданными пользователем.
- **Результат:** ресурс Open WebUI добавлен в список карточек вкладки «Полезные ресурсы» и открывается в новой вкладке.
- **Статус:** success

### Шаг 126
- **Задача:** Добавить ресурс «Админка Outlook» во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:11 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `Админка Outlook` с переданной ссылкой на OWA/ECP и изображением от пользователя.
- **Результат:** карточка «Админка Outlook» отображается во вкладке ресурсов и открывает внешний сайт в новой вкладке.
- **Статус:** success

### Шаг 127
- **Задача:** Добавить ресурс YouTrack во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:13 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `YouTrack` с пользовательской ссылкой авторизации и присланным изображением.
- **Результат:** карточка YouTrack добавлена в ресурсы и открывает сайт в новой вкладке.
- **Статус:** success

### Шаг 128
- **Задача:** Добавить ресурс Graylog во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:19 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `Graylog` со ссылкой `https://new-graylog.corp.hpdd.ru/welcome` и изображением, переданными пользователем.
- **Результат:** карточка Graylog отображается во вкладке ресурсов и открывает внешний сайт в новой вкладке.
- **Статус:** success

### Шаг 129
- **Задача:** Добавить ресурс «КиберБэкап» во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:22 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `КиберБэкап` со ссылкой `https://hpdd-bcp-mng02.corp.hpdd.ru:9877/` и изображением, переданными пользователем.
- **Результат:** карточка «КиберБэкап» добавлена во вкладку ресурсов и открывает внешний сайт в новой вкладке.
- **Статус:** success

### Шаг 130
- **Задача:** Добавить ресурс «Админка Битрикс» во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:35 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `Админка Битрикс` со ссылкой `https://portal.hpdd.ru/bitrix/admin/user_edit.php?lang=ru&ID=3582&user_edit_active_tab=edit1#authorize` и изображением от пользователя.
- **Результат:** карточка «Админка Битрикс» отображается в разделе ресурсов и открывается в новой вкладке.
- **Статус:** success

### Шаг 131
- **Задача:** Оставить во вкладке «Полезные ресурсы» только карточки, присланные пользователем, и убрать битые превью.
- **Время:** 2026-05-08 11:39 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` из `usefulResources` удалены карточки, которые пользователь не присылал (`Битрикс24`, `Outlook Web`, `Zoom`, `Confluence`, `Jira`, `GitLab`); для `.resource-image` добавлен `error`-fallback на SVG-превью (буква+градиент), если исходное изображение недоступно.
- **Результат:** в списке остались только пользовательские ресурсы; вместо «битых» картинок отображаются корректные fallback-превью.
- **Статус:** success

### Шаг 132
- **Задача:** Починить загрузку пользовательских картинок в карточках «Полезные ресурсы».
- **Время:** 2026-05-08 11:43 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** скопировал переданные изображения в статическую папку фронта `ProjectTP/frontend/resource-images/` (файлы: `open-webui.png`, `outlook-admin.png`, `youtrack.png`, `graylog.png`, `cyber-backup.png`, `bitrix-admin.png`); в `ProjectTP/frontend/app.js` заменил абсолютные служебные пути Cursor на относительные `./resource-images/...`.
- **Результат:** карточки используют локальные статические файлы приложения, поэтому изображения стабильно загружаются в браузере.
- **Статус:** success

### Шаг 133
- **Задача:** Сделать карточки «Полезные ресурсы» немного компактнее.
- **Время:** 2026-05-08 11:44 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/styles.css` уменьшил минимальную ширину карточек в сетке (`220px` → `200px`), уменьшил высоту превью через `aspect-ratio` (`16/9` → `16/8`) и сократил внутренние отступы блока контента (`12px` → `10px`).
- **Результат:** карточки визуально стали меньше и на экране помещается больше ресурсов.
- **Статус:** success

### Шаг 134
- **Задача:** Добавить ресурс HRLink во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:48 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** изображение ресурса сохранено в `ProjectTP/frontend/resource-images/hrlink.png`; в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `HRLink` со ссылкой `https://lk.hr-link.ru/employee`.
- **Результат:** карточка HRLink отображается в разделе ресурсов и открывается в новой вкладке.
- **Статус:** success

### Шаг 135
- **Задача:** Добавить ресурс «Админка Zoom» во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 11:50 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** изображение ресурса сохранено в `ProjectTP/frontend/resource-images/zoom-admin.png`; в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `Админка Zoom` со ссылкой `https://hpdd-ru.zoom.us/myhome`.
- **Результат:** карточка «Админка Zoom» добавлена в список ресурсов и открывает внешний сайт в новой вкладке.
- **Статус:** success

### Шаг 136
- **Задача:** Добавить ресурс ServiceDesk во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 13:29 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** изображение ресурса сохранено в `ProjectTP/frontend/resource-images/servicedesk.png`; в `ProjectTP/frontend/app.js` в массив `usefulResources` добавлена карточка `ServiceDesk` со ссылкой `https://esd.hpdd.ru/HomePage.do?view_type=my_view`.
- **Результат:** карточка ServiceDesk отображается в разделе ресурсов и открывает внешний сайт в новой вкладке.
- **Статус:** success

### Шаг 137
- **Задача:** Выполнить `TASKS.md` №40 — календарный режим графика (неделя/месяц).
- **Время:** 2026-05-08 13:34 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` добавлены переключатель вида (`Таблица/Неделя/Месяц`) и блок календаря; в `ProjectTP/frontend/app.js` реализованы режимы `dutyViewMode`, расчёт диапазона недели/месяца, загрузка данных по датам через существующий `GET /api/duties`, рендер карточек дней и обновление при смене даты/вида/сохранении; в `ProjectTP/frontend/styles.css` добавлены стили календарной сетки и карточек; обновлён `TASKS.md` (`№40` → выполнено).
- **Результат:** пользователь переключает график между таблицей и календарём; недельный/месячный вид показывают назначения по датам без изменений backend API.
- **Статус:** success

### Шаг 138
- **Задача:** Выполнить `TASKS.md` №41 — раздельные права в админке (делегирование без полной роли admin).
- **Время:** 2026-05-08 13:39 +0300
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в backend добавлены поля прав пользователя (`can_manage_duties`, `can_manage_reports`, `can_manage_notifications`) в `ProjectTP/backend/app/models.py` и миграция `ProjectTP/backend/alembic/versions/p1q2r3s4t5u6_user_capabilities.py`; расширены схемы (`permissions` в `UserOut`/`UserMeOut`), добавлены проверки прав в `ProjectTP/backend/app/deps.py` (`user_has_capability`, `require_capability`, `require_admin_or_any_capability`), endpoint `PATCH /api/admin/users/{id}/permissions` в `ProjectTP/backend/app/routers/admin_users.py`, привязка прав к маршрутам графика/уведомлений/экспортов и логике отчётов (`duties_live.py`, `exports.py`, `reports.py`), обновлены `auth.py` и выдача `/api/me`; во frontend (`ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`) добавлена колонка делегированных прав в таблице сотрудников и учёт capability-флагов для доступа к разделам/действиям.
- **Результат:** можно делегировать отдельные функции (график, отчёты, уведомления) пользователям support без выдачи полной роли admin; существующие сценарии полного admin сохранены; `TASKS.md` №41 отмечена выполненной.
- **Статус:** success

### Шаг 139
- **Задача:** Выполнить `TASKS.md` №42 — редактируемые шаблоны уведомлений.
- **Время:** 2026-05-08 13:40 +0300
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в `ProjectTP/backend/app/models.py` расширил `DutyNotificationSettings` полями шаблонов (`upcoming_5m_template`, `start_personal_template`, `start_chat_template`, `test_with_slot_template`, `test_without_slot_template`) и добавил миграцию `ProjectTP/backend/alembic/versions/v7w8x9y0z1a2_notification_templates.py`; в `ProjectTP/backend/app/schemas.py` добавил `DutyNotificationTemplatesOut` и `DutyNotificationTemplatesUpdateRequest`; в `ProjectTP/backend/app/duty_notifications.py` реализовал преобразование/сохранение шаблонов и рендер через `str.format` с валидацией переменных, подключил шаблоны в боевые и тестовые уведомления; в `ProjectTP/backend/app/routers/duties_live.py` добавил API `GET/PATCH /api/admin/notifications/templates`; во frontend (`ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`) добавил форму редактирования шаблонов в карточке «Уведомления», загрузку при инициализации и сохранение кнопкой.
- **Результат:** пользователь с правом `manage_notifications` может менять тексты уведомлений из UI без правки кода; backend применяет сохранённые шаблоны при отправке.
- **Статус:** success

### Шаг 140
- **Задача:** Выполнить `TASKS.md` №43 — мини-аналитика дежурств и обменов.
- **Время:** 2026-05-08 13:54 +0300
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в `ProjectTP/backend/app/schemas.py` добавил схемы ответа аналитики (`DutyAnalyticsOut`, `DutySwapAnalyticsOut`, `DutyAnalyticsEmployeeRowOut`); в `ProjectTP/backend/app/routers/reports.py` добавил endpoint `GET /api/admin/analytics/duties-swaps` с доступом по праву `manage_reports`, который агрегирует за период: ёмкость слотов, назначенные/незаполненные слоты, распределение по сотрудникам и статистику обменов по статусам; во frontend (`ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`) добавил карточку «Мини-аналитика» в админке (период + сводка + список сотрудников), загрузку по кнопке и авто-загрузку при входе в интерфейс для пользователей с правом отчётов.
- **Результат:** доступен лёгкий аналитический блок за выбранный период без новых сущностей БД и без усложнения конфигурации.
- **Статус:** success

### Шаг 141
- **Задача:** Добавить под-разделы внутри вкладки «Админ» (аналитика, генерация графика, уведомление, сотрудники).
- **Время:** 2026-05-08 14:35 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` добавил вторую панель вкладок в секции `Админ` (`.admin-subtab-btn`), связав её с уже существующими карточками (`adminAnalyticsCard`, `adminGenerateCard`, `adminNotificationsCard`, `adminUsersCard`); в `ProjectTP/frontend/app.js` добавил логику `activateAdminSubtab`/`syncAdminSubtabs`, которая переключает под-разделы и учитывает права пользователя (скрывает недоступные кнопки и выбирает первый доступный раздел); в обработчике верхних вкладок добавил защиту от кликов по под-вкладкам без `data-tab`; обновил версию статики в `ProjectTP/frontend/index.html` (`v=20260508-admin-subtabs`) для сброса кэша.
- **Результат:** во вкладке «Админ» появился удобный второй уровень навигации по 4 под-разделам без изменения backend API.
- **Статус:** success

### Шаг 142
- **Задача:** Переименовать режим вида графика `Таблица` в `День`.
- **Время:** 2026-05-08 14:37 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` в селекте `#dutiesViewMode` изменил подпись опции `value="table"` с `Таблица` на `День`, не меняя внутреннее значение для сохранения совместимости текущей логики в `app.js`.
- **Результат:** в UI выбор режимов отображается как `День / Неделя / Месяц`.
- **Статус:** success

### Шаг 143
- **Задача:** Убрать необходимость кнопки «Обновить» при переключении вида графика и подсветить текущего пользователя в календаре.
- **Время:** 2026-05-08 14:39 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/app.js` для `#dutiesViewMode` добавил автоматический вызов `loadDutiesCalendar(dateStr)` при смене режима `День/Неделя/Месяц` (теперь календарный вид обновляется сразу без ручной кнопки); в построении данных календаря добавил флаг `isSelf` для строк текущего пользователя и в рендере недельного/месячного списка применил CSS-класс `duty-day-list-self`; в `ProjectTP/frontend/styles.css` добавил стиль подсветки (зелёный фон + акцентная полоса) для личных слотов в календаре.
- **Результат:** при смене вида график/календарь загружается автоматически, а дежурства текущего пользователя заметно подсвечены в календарных карточках.
- **Статус:** success

### Шаг 144
- **Задача:** Добавить ресурс `Proxy admin` во вкладку «Полезные ресурсы».
- **Время:** 2026-05-08 14:42 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** скопировал присланное изображение в `ProjectTP/frontend/resource-images/proxy-admin.png`; в `ProjectTP/frontend/app.js` в массив `usefulResources` добавил карточку `Proxy admin` со ссылкой `https://proxy-portal.hpdd.ru/`.
- **Результат:** в разделе ресурсов появилась карточка Proxy admin с пользовательским изображением и переходом на указанный сайт в новой вкладке.
- **Статус:** success

### Шаг 145
- **Задача:** Добавить роль `designer` и подготовить варианты редизайна фронта.
- **Время:** 2026-05-08 15:31 +0300
- **Кто исполнил:** orchestrator + designer (subagent)
- **Как исполнил:** в блоке ролей `HANDOFF.md` добавил роль `designer`; запустил дизайнерского сабагента на аудит текущего фронта (`index.html`, `styles.css`, `app.js`) и оформил 3 версии дизайна с рекомендацией внедрения в отдельный документ.
- **Результат:** в процессе оркестрации доступен специализированный агент `designer`; команда получила 3 практичных варианта дизайна фронта и рекомендованный путь внедрения.
- **Артефакт:** `ProjectTP/docs/frontend-design-variants.md`
- **Статус:** success

### Шаг 146
- **Задача:** Сформировать backlog-задачи из отчёта дизайнера и удалить временный отчёт.
- **Время:** 2026-05-08 15:44 +0300
- **Кто исполнил:** orchestrator
- **Как исполнил:** на основе `ProjectTP/docs/frontend-design-variants.md` добавил в `TASKS.md` новые задачи `№44-№48` (Balanced-токены, унификация компонентов, правки `График/Отчеты`, читаемость `Админ`, поиск в `Полезные ресурсы`); после переноса в бэклог удалил временный отчёт дизайнера.
- **Результат:** рекомендации дизайнера переведены в управляемые задачи бэклога, промежуточный документ удалён по запросу пользователя.
- **Статус:** success

### Шаг 147
- **Задача:** Выполнить `TASKS.md` №44 (UI-токены Balanced).
- **Время:** 2026-05-08 15:48 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/styles.css` обновил токены в `:root` под Balanced-стиль (палитра, типографика, бордеры, тени, focus-ring), выровнял базовую контрастность фона/топбара и добавил явные стили `btn.secondary`/`btn.ghost`; в `ProjectTP/frontend/index.html` и `ProjectTP/frontend/login.html` обновил версию статики CSS для сброса кэша.
- **Результат:** база UI-токенов зафиксирована в едином стиле Balanced без изменения бизнес-логики фронта; задача №44 переведена в статус выполнено.
- **Артефакт:** `ProjectTP/frontend/styles.css`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `TASKS.md`
- **Статус:** success

### Шаг 148
- **Задача:** Выполнить `TASKS.md` №45 (унификация базовых UI-компонентов).
- **Время:** 2026-05-08 15:50 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/styles.css` унифицировал ритм компонентов: добавил общий `--control-height`, выровнял высоту/паддинги для `btn` и полей, привёл вкладки к единой плотности, сделал `admin`-подвкладки pill-стилем, усилил таблицы (`zebra`, `sticky thead`, рамка `table-wrap`) и сообщения (`msg` с акцентной левой границей).
- **Результат:** базовые компоненты (`btn/card/tabs/table/msg`) визуально консистентны между разделами, без изменения JS/API-логики; задача №45 переведена в статус выполнено.
- **Артефакт:** `ProjectTP/frontend/styles.css`, `TASKS.md`
- **Статус:** success

### Шаг 149
- **Задача:** Выполнить `TASKS.md` №46-№48 (иерархия `График/Отчеты`, читаемость `Админ`, поиск ресурсов).
- **Время:** 2026-05-08 16:10 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `ProjectTP/frontend/index.html` усилил UI-зоны `График/Отчеты` (toolbar-контейнеры, акценты кнопок действий) и добавил поле `resourcesSearchInput`; в `ProjectTP/frontend/styles.css` добавил стили для новых toolbar-зон, подчёркнутого блока текущего дежурного, result-zone отчётов и более читаемой структуры секций `Админ`; в `ProjectTP/frontend/app.js` реализовал клиентскую фильтрацию карточек по названию/описанию с реакцией на `input`; обновил cache-bust версии `app.js` в `index.html` и `login.html`.
- **Результат:** экраны `График` и `Отчеты` визуально разделены на зоны управления и результата; вкладка `Админ` читается блоками; во вкладке `Полезные ресурсы` работает live-поиск без backend-изменений; задачи №46, №47 и №48 переведены в статус выполнено.
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/styles.css`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/login.html`, `TASKS.md`
- **Статус:** success

### Шаг 150
- **Задача:** Выполнить post-change smoke-check после UI-итерации.
- **Время:** 2026-05-08 16:13 +0300
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** проверил синтаксис фронтенда командой `node --check ProjectTP/frontend/app.js`; дополнительно попытался проверить runtime-статус `docker compose ps`, но из текущей сессии доступ к Docker socket отсутствует (нужны права/`sudo` в интерактивном терминале).
- **Результат:** статическая проверка прошла успешно; runtime-проверка в этой сессии ограничена правами окружения.
- **Статус:** partial

### Шаг 151
- **Задача:** Завершить `TASKS.md` №39 («Полезные ресурсы») по фактическому состоянию фронта.
- **Время:** 2026-05-08 16:15 +0300
- **Кто исполнил:** orchestrator
- **Как исполнил:** сверил критерии задачи №39 с текущей реализацией (`index.html`, `app.js`, `styles.css`): вкладка присутствует в меню, карточки рендерятся из фронтового списка, ссылки открываются в новой вкладке с `rel="noopener noreferrer"`, сетка карточек адаптивная; после сверки перевёл задачу из статуса `В очереди` в `Выполнено` и добавил артефакты в карточку задачи.
- **Результат:** в `TASKS.md` больше нет задач со статусом `В очереди`; UI-блок задач №39-№48 синхронизирован с фактическим состоянием проекта.
- **Артефакт:** `TASKS.md`
- **Статус:** success

### Шаг 152
- **Задача:** Синхронизировать `HANDOFF.md` с обновлённой дорожной картой после закрытия бэклога `TASKS.md`.
- **Время:** 2026-05-13 12:00 +0300
- **Кто исполнил:** агент (по запросу пользователя)
- **Как исполнил:** обновлены блоки **Якорь ACTIVE**, **Текущий статус задачи** и **Следующий шаг**: снят устаревший фокус на P1 №1 и №38; текущее состояние выровнено с `ProjectTP/docs/roadmap.md` (эксплуатация уведомлений, опциональная приёмка, пополнение бэклога при новых требованиях).
- **Результат:** следующий исполнитель не воспринимает закрытые задачи как «текущий блокер»; приоритет явно смещён к стенду и настройкам окружения согласно roadmap.
- **Артефакт:** `HANDOFF.md`, `ProjectTP/docs/roadmap.md`
- **Статус:** success

### Шаг 153
- **Задача:** Пополнить бэклог новыми карточками после закрытия задач до №48.
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** агент (по запросу пользователя)
- **Как исполнил:** в корневый `TASKS.md` добавлена секция «Бэклог» с задачами **№49–№66** (статус «Не начато»); обновлены `ProjectTP/docs/roadmap.md`, блоки **Якорь ACTIVE** и **Текущий статус задачи** в `HANDOFF.md`.
- **Результат:** открытые задачи разработки снова отражены в репозитории; приоритет P1 — **№49** (МСК).
- **Артефакт:** `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 154
- **Задача:** Выполнить `TASKS.md` №49 (единый эталон МСК для уведомлений и слотов).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** добавлен `ProjectTP/backend/app/duty_tz.py` (`anchor_dispatch_at`, `today_moscow`); расчёт слота в `duty_notification_slot.py` / `duty_notifications.py` переведён на МСК; дефолт `TZ=Europe/Moscow` в `config.py`, `notification_scheduler.py`, `docker-compose.yml`; «сегодня» для теста уведомления и графика в Битрикс — по МСК (`duties_live.py`, `send_schedule_bitrix.py`); обновлены `ProjectTP/docs/notifications-inventory.md`, `notifications-diagnostics.md`, добавлен `ProjectTP/docs/duty-time-moscow.md`; вычищен дубликат `TZ` в `.env.example`; в `TASKS.md` задача №49 — выполнено.
- **Результат:** триггеры и сопоставление слотов не зависят от TZ браузера; типичный деплой с `TZ`/`Europe/Moscow` согласован с документацией.
- **Артефакт:** `ProjectTP/backend/app/duty_tz.py`, `ProjectTP/backend/app/config.py`, `ProjectTP/backend/app/duty_notification_slot.py`, `ProjectTP/backend/app/duty_notifications.py`, `ProjectTP/backend/app/notification_scheduler.py`, `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/send_schedule_bitrix.py`, `ProjectTP/docker-compose.yml`, `ProjectTP/.env.example`, `ProjectTP/docs/duty-time-moscow.md`, `ProjectTP/docs/notifications-inventory.md`, `ProjectTP/docs/notifications-diagnostics.md`, `ProjectTP/backend/tests/test_duty_notification_slot.py`, `TASKS.md`
- **Статус:** success

### Шаг 155
- **Задача:** Добавить в бэклог задачу на исправление генерации графика при выборе нескольких дней.
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** агент (по запросу пользователя)
- **Как исполнил:** в корневый `TASKS.md` добавлена карточка **№67** (одинаковое распределение на все дни периода); обновлены `ProjectTP/docs/roadmap.md`, блоки **Якорь ACTIVE**, **Текущий статус задачи**, **Следующий шаг** в `HANDOFF.md`.
- **Результат:** дефект генерации отражён как **P1**; диапазон открытого бэклога **№50–№67**.
- **Артефакт:** `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 156
- **Задача:** Выполнить `TASKS.md` №50 (runbook эксплуатации уведомлений: env и БД).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** devops/docs (агент)
- **Как исполнил:** добавлен `ProjectTP/docs/notifications-runbook.md` (таблица переменных окружения, расшифровка `duty_notification_settings`, сценарии APScheduler vs внешний cron, чеклист); в `deploy-rocky-linux.md` — подраздел **4.1** со ссылкой на runbook; в `notifications-inventory.md` исправлено описание админских настроек (фактическое поведение `scheduler_enabled`, `selected_method`, чтение `cron_enabled_*`); обновлены `roadmap.md`, `TASKS.md`; строка **ACTIVE** в `HANDOFF.md` — на **№51** / **№67**.
- **Результат:** команда может воспроизвести настройку уведомлений по документам в репозитории без устных пояснений.
- **Артефакт:** `ProjectTP/docs/notifications-runbook.md`, `ProjectTP/docs/notifications-inventory.md`, `ProjectTP/docs/deploy-rocky-linux.md`, `ProjectTP/docs/roadmap.md`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 157
- **Задача:** Выполнить `TASKS.md` №51 (регламент исключения дублей между каналами уведомлений).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** devops/docs (агент)
- **Как исполнил:** добавлен `ProjectTP/docs/notifications-dedup.md` (матрицы инициаторов и исходящих каналов, золотое правило «один источник расписания», профили A/B/C, чеклист стенда, явно зафиксировано отсутствие idempotency в коде); перекрёстные ссылки в `notifications-runbook.md`, `notifications-inventory.md`, `notifications-diagnostics.md`, `roadmap.md`; в `TASKS.md` задача №51 — выполнено; обновлены **ACTIVE** и блоки статуса в `HANDOFF.md`.
- **Результат:** типичная конфигурация описывается без двусмысленности; дубли предотвращаются регламентом и проверкой стенда.
- **Артефакт:** `ProjectTP/docs/notifications-dedup.md`, `ProjectTP/docs/notifications-runbook.md`, `ProjectTP/docs/notifications-inventory.md`, `ProjectTP/docs/notifications-diagnostics.md`, `ProjectTP/docs/roadmap.md`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 158
- **Задача:** Выполнить `TASKS.md` №52 (чеклист приёмки на стенде).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** qa/docs (агент)
- **Как исполнил:** добавлен `ProjectTP/docs/staging-acceptance-checklist.md` (вход, график таблица/календарь, отчёты, админ, опционально уведомления и обмен; таблица результата прогона и вердикт); ссылки в `roadmap.md`, `deploy-rocky-linux.md` (§4.1), `notifications-runbook.md`; в `TASKS.md` задача №52 — выполнено; **ACTIVE** и блоки статуса в `HANDOFF.md` обновлены под **№53** / **№67**.
- **Результат:** команда может зафиксировать прогон приёмки в репозитории или скопировать таблицу во внешний тикет.
- **Артефакт:** `ProjectTP/docs/staging-acceptance-checklist.md`, `ProjectTP/docs/roadmap.md`, `ProjectTP/docs/deploy-rocky-linux.md`, `ProjectTP/docs/notifications-runbook.md`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 159
- **Задача:** Выполнить `TASKS.md` №67 (многодневная генерация графика — одинаковое распределение по дням).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** в `duty_generation.run_generation` после обработки всех слотов дня добавлен `tx_db.flush()` (сессия с `autoflush=False` иначе не видела вчерашние INSERT в той же транзакции при `_yesterday_counts`); расширен модульный комментарий; в `check_generation_fairness.py` — проверка уникальности векторов назначений по рабочим дням при пуле ≥12 человек; обновлены `TASKS.md`, `roadmap.md`, блоки **ACTIVE**/статус/**Следующий шаг** в `HANDOFF.md`.
- **Результат:** учёт «вчера» внутри одного прогона генерации согласован с данными в БД-сессии; регресс ловится smoke-скриптом на стенде с достаточным пулом.
- **Артефакт:** `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/check_generation_fairness.py`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 160
- **Задача:** Выполнить `TASKS.md` №53 (праздники РФ при автогенерации графика).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** добавлены `app/duty_rf_holidays.py` (выходные + `holidays.Russia`), зависимость `holidays` в `requirements.txt`, генерация пропускает такие дни как выходные; общий хелпер `ProjectTP/rf_calendar_for_checks.py` для smoke-скриптов; обновлены `check_generation_fairness.py`, `check_generation_constraints.py`, `duty_generation_fairness.md`, `index.html`, тесты `tests/test_duty_rf_holidays.py`; `TASKS.md`, `roadmap.md`, **ACTIVE** в `HANDOFF.md`.
- **Результат:** на официальные нерабочие дни РФ слоты автогенерацией не создаются; сокращённые предпраздничные дни остаются рабочими для слотов.
- **Артефакт:** `ProjectTP/backend/app/duty_rf_holidays.py`, `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/backend/requirements.txt`, `ProjectTP/rf_calendar_for_checks.py`, `ProjectTP/check_generation_fairness.py`, `ProjectTP/check_generation_constraints.py`, `ProjectTP/duty_generation_fairness.md`, `ProjectTP/frontend/index.html`, `ProjectTP/backend/tests/test_duty_rf_holidays.py`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 161
- **Задача:** Выполнить `TASKS.md` №54 (копирование периода графика как шаблон).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** добавлены `duty_copy.slot_updates_for_copy`, эндпоинт `POST /api/duties/copy-range` (проверка длины диапазонов, лимит слотов на человека, те же правила пользователей что у batch); в админке — блок «Копирование шаблона» с датами источника/цели и флагом перезаписи; дефолты — прошлый и текущий месяц; общая проверка назначаемых вынесена в `_assert_assignees_for_duty_day` и переиспользована в `duties_batch`; тесты `test_duty_copy_range.py`; обновлены `TASKS.md`, `roadmap.md`, **ACTIVE** и статус в `HANDOFF.md`.
- **Результат:** админ может перенести график по дням на новый период той же длины и доработать вручную.
- **Артефакт:** `ProjectTP/backend/app/duty_copy.py`, `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/backend/tests/test_duty_copy_range.py`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 162
- **Задача:** Выполнить `TASKS.md` №55 (фильтры отображения графика и календаря).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** на вкладке «График» добавлен селектор «Показ»: все слоты, только мои, только пустые (опция пустых — при `manage_duties` через `syncDutyFilterOptions`); таблица дня скрывает лишние строки (`tr.hidden`), сохранение графика по-прежнему шлёт все слоты; неделя/месяц кэшируют ответы `/api/duties` в `dutyCalendarRaw`, смена фильтра перерисовывает календарь без повторных GET; подписи дней и пустые состояния зависят от фильтра; стиль `.duty-day-list-unassigned`; обновлены `TASKS.md`, `roadmap.md`, **ACTIVE** в `HANDOFF.md`.
- **Результат:** support и admin видят нужный срез графика без изменения контракта API и без лишней нагрузки при переключении фильтра в календаре.
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 163
- **Задача:** Выполнить `TASKS.md` №56 (экспорт графика за период).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** добавлены `duty_export.build_duties_period_excel_bytes` (лист «График»: даты × слоты, ФИО из БД, «—» для пустых) и `GET /api/duties/export-period` с `manage_duties`, лимит 120 дней; в админке — кнопка «Скачать Excel за период» (даты из полей генерации), сообщение `exportDutiesMsg`; PDF не делался; обновлены `TASKS.md`, `roadmap.md`, **ACTIVE** и статус в `HANDOFF.md`.
- **Результат:** happy-path скачивание .xlsx за выбранный диапазон без расхождения с данными графика в БД.
- **Артефакт:** `ProjectTP/backend/app/duty_export.py`, `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 164
- **Задача:** Выполнить `TASKS.md` №57 (расширение мини-аналитики).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в ответ `GET /api/admin/analytics/duties-swaps` добавлены `monthly` (слоты и обмены по календарным месяцам в диапазоне, `extract` year/month) и `overload_warnings` (при ≥12 назначениях и ≥2 сотрудниках — порог max(среднее×1.35, ⌈среднее⌉+2)); UI — компактная таблица по месяцам, полоски доли среди сотрудников, блок предупреждений и пояснение эвристики; стили в `styles.css`; обновлены `TASKS.md`, `roadmap.md`, **ACTIVE** и статус в `HANDOFF.md`.
- **Результат:** тренды и распределение читаются без перегруза интерфейса; цифры согласованы с запросами к `DutyAssignment` и `DutySwapRequest`.
- **Артефакт:** `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/app/routers/reports.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 165
- **Задача:** Закрыть бэклог **№58–№66** одним проходом (инструкция по блокам, CI, тесты, health/ready, лимит входа и сессии, a11y focus, пустые состояния, документация модульности и статики).
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend + frontend + devops/docs (агент)
- **Как исполнил:** добавлены `employee_exit_blocks.py`, поле `blocks` в API/UI для инструкции выхода; `.gitlab-ci.yml` с pytest; `test_employee_exit_blocks.py`; `GET /api/ready`, расширен `/api/health`; in-memory throttling входа и `SESSION_MAX_AGE_SECONDS`; `:focus-visible` в CSS; bump `?v=` статики; README и `.env.example`; карточки **№58–№66** в `TASKS.md` — выполнено; **ACTIVE**/статус/`roadmap.md`.
- **Результат:** открытых карточек **№58–№66** нет; **№49–№67** закрыты до пополнения бэклога.
- **Артефакт:** перечисленные файлы + `HANDOFF.md`, `TASKS.md`, `ProjectTP/docs/roadmap.md`, `ProjectTP/README.md`, `ProjectTP/.env.example`, `ProjectTP/frontend/login.html`
- **Статус:** success

### Шаг 166
- **Задача:** Убрать журнал «кто и когда входил» из мини-аналитики; показывать **последний вход** в админке «Сотрудники»; подтянуть вёрстку таблицы; не оставлять сломанный CSS страницы входа.
- **Время:** 2026-05-13 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** удалены эндпоинт `GET /api/admin/analytics/site-logins`, схема `SiteLoginEventOut`, фронт (`apiGetSiteLogins`, блок аналитики); в `User` добавлено `last_login_at`, при успешном `POST /api/login` обновление + `UserOut`/`UserMeOut`; миграция `y7z8a9b0c1d2` — колонка, backfill из `site_login_events` при наличии, `DROP` таблицы; в таблице сотрудников колонка «Последний вход», классы `admin-users-editor-table`, `btn-compact`, подпись в UI; в `styles.css` восстановлен блок `.auth-page` (сломанный селектор после `.duty-day-list-unassigned`); bump `?v=` у `styles.css`/`app.js` в `index.html` и `login.html`.
- **Результат:** аналитика без списка входов; последний вход виден в списке пользователей; форма входа снова по центру.
- **Артефакт:** `ProjectTP/backend/app/models.py`, `ProjectTP/backend/app/routers/auth.py`, `ProjectTP/backend/app/routers/reports.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/app/routers/admin_users.py`, `ProjectTP/backend/alembic/versions/y7z8a9b0c1d2_user_last_login_drop_site_events.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `HANDOFF.md`
- **Статус:** success

### Шаг 167
- **Задача:** Обновить фронт под приложенный мокап: убрать верхнее горизонтальное меню, добавить сворачиваемый левый сайдбар и dashboard-компоновку экрана «График дежурств».
- **Время:** 2026-05-14 (сессия диалога)
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** `index.html` переведён на `app-shell` с левым `sidebar` (иконки по умолчанию, подписи при hover/focus), верхняя панель оставлена только для пользователя/роли/выхода; `employeeExitOpenBtn` перенесён в настройки; экран графика собран из hero «Сегодня дежурит», часов, быстрых действий, фильтров, таблицы и правой колонки с `swapCard` + placeholder «Недавняя активность»; JS добавляет clock, заголовок расписания, initials, badge запросов и focus quick actions; CSS — новый layout/sidebar/cards/responsive, bump `?v=20260514-sidebar-dashboard`.
- **Результат:** внешний вид приведён ближе к мокапу без изменения бизнес-API и контрактов данных.
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`, `ProjectTP/frontend/login.html`, `HANDOFF.md`
- **Статус:** success

### Шаг 168
- **Задача:** Выполнить `TASKS.md` **№68–№71**: вкладка «Админ» только для `role === "admin"`, текст hero «Сейчас дежурит» без чипа статуса, убрать «Назначить дежурство», кнопка «Запросить замену» → сообщение в Битрикс.
- **Время:** 2026-05-14 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** `setAdminMode` скрывает `#adminTabBtn` по `state.isRootAdmin`; `activateTab` откатывает `admin` на `graph` для не-admin; `POST /api/me/duty-replacement-request/bitrix` (тот же чат, что график) с текстом про замену и `bitrix_im_display_name`; фронт — `apiMeNotifyDutyReplacementBitrix` + сообщения в `#dutiesMsg`; тест `test_duty_replacement_bitrix_notify.py`; карточки **№68–№71** в `TASKS.md`; bump `?v=` у `app.js`.
- **Результат:** Support с делегированными правами не видит вкладку «Админ», но сохраняет `.admin-only` на графике/отчётах; сотрудник может отправить запрос замены в Битрикс одной кнопкой.
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/tests/test_duty_replacement_bitrix_notify.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 169
- **Задача:** Запрос замены дежурства в Битрикс — не в общий чат, а **личные сообщения всем администраторам** (роль admin + Bitrix ID в карточке).
- **Время:** 2026-05-14 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** `POST /api/me/duty-replacement-request/bitrix` выбирает `User.role == "admin"` с непустым `bitrix_user_id`, шлёт `im.message.add` на каждый ID через `BITRIX_INCOMING_WEBHOOK_URL`; ответ `recipients_bitrix`; при отсутствии URL или админов с ID — 400 с пояснением; тест на мок БД+вебхука; текст успеха во фронте; **ACTIVE**, карточка **№71** в `TASKS.md`.
- **Результат:** уведомление получают только админы с привязкой к Битрикс, а не весь чат графика.
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/tests/test_duty_replacement_bitrix_notify.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 170
- **Задача:** Выполнить `TASKS.md` **№72**: форма обмена дежурствами — подписи слотов по графику; уведомление в Битрикс второму участнику при создании заявки.
- **Время:** 2026-05-14 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** `refreshSwapSlotSelects()` подгружает `/api/duties` на дату обмена; «Мой слот» — только слоты текущего пользователя с подписью `slotRangeLabel`; «Слот другого» — время + ФИО; `notify_duty_swap_target_bitrix` после commit в `duty_swaps.py` (ЛС, ошибки вебхука только в лог); тесты `test_swap_bitrix_notify.py`; bump `?v=` у `app.js`; карточка **№72**, **ACTIVE**.
- **Результат:** Второй сотрудник видит понятные варианты слотов и получает ЛС в Битрикс при предложении обмена (если задан `bitrix_user_id`).
- **Артефакт:** `ProjectTP/backend/app/routers/duty_swaps.py`, `ProjectTP/backend/tests/test_swap_bitrix_notify.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 171
- **Задача:** Выполнить `TASKS.md` **№73**: UI-полировка второстепенных вкладок после визуального прохода сайта.
- **Время:** 2026-05-18 15:42 +03:00
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** облегчённый старт без полного обзора: пользователь выбрал UI-review, затронуты только `ProjectTP/frontend/`; вкладки «Полезные ресурсы» и «Настройки пользователя» переведены на `secondary-page`/карточную оболочку, настройки собраны в сетку карточек; версии `app.js`/`styles.css` синхронизированы в `index.html` и `login.html`; карточка **№73** добавлена в `TASKS.md`.
- **Результат:** второстепенные экраны визуально ближе к dashboard-компоновке главной страницы без изменения API и бизнес-логики; `node --check ProjectTP/frontend/app.js`, HTML parse и ReadLints пройдены.
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 172
- **Задача:** Выполнить `TASKS.md` **№74**: обновить страницу «Полезные ресурсы» по приложенному мокапу.
- **Время:** 2026-05-18 15:55 +03:00
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `index.html` собрана dashboard-компоновка вкладки с основным блоком и правой панелью; в `app.js` существующий список ресурсов дополнен категориями, клиентской фильтрацией и статичными списками «Избранные»/«Недавно открывали» без нового API; в `styles.css` добавлены карточки, бейджи, фильтры, правые списки и адаптив; версии статики обновлены в `index.html`/`login.html`; карточка **№74** добавлена в `TASKS.md`.
- **Результат:** список ресурсов и ссылки сохранены, карточки открываются в новой вкладке, активный раздел остаётся в левом сайдбаре, верхняя панель не расширялась; `node --check ProjectTP/frontend/app.js`, HTML parse и ReadLints пройдены.
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 173
- **Задача:** Выполнить `TASKS.md` **№75**: разделить в аналитике обычные и утренние дежурства.
- **Время:** 2026-05-18 16:05 +03:00
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** в `reports.py` аналитика разделяет слоты 07:00/08:00 (`MORNING_SLOT_INDEXES`) и остальные слоты для capacity, назначений, помесячной агрегации, нагрузки и перегруза; схемы ответа расширены обратно совместимыми полями; фронт админ-аналитики показывает отдельные KPI, месяцы, два списка нагрузки и раздельный риск; версии статики обновлены; `roadmap.md` и `TASKS.md` синхронизированы.
- **Результат:** утренние дежурства больше не попадают в счётчики обычных; `py_compile`, `node --check`, HTML parse и ReadLints пройдены; pytest/import backend заблокированы отсутствием локальных зависимостей (`pytest`, `fastapi`).
- **Артефакт:** `ProjectTP/backend/app/routers/reports.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `ProjectTP/docs/roadmap.md`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 174
- **Задача:** Выполнить **№76**: рабочие «Избранные» и «Недавно открывали» на вкладке «Полезные ресурсы».
- **Время:** 2026-05-18 16:20 +03:00
- **Кто исполнил:** frontend (агент)
- **Как исполнил:** в `app.js` — ключи `tpp.resources.favorites.{userId}` и `tpp.resources.recent.{userId}`, toggle ★, запись истории при клике, фильтр `state.resourceGridFilter`, seed избранного при первом визите; в `index.html` — кнопки «Смотреть все…»; стили `.resource-favorite-btn`; bump `?v=20260518-resources-functional`; карточка **№76** в `TASKS.md`.
- **Результат:** избранное и недавние сохраняются в браузере без API; `node --check ProjectTP/frontend/app.js` пройден.
- **Артефакт:** `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `TASKS.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 175
- **Задача:** Добавить полезный ресурс «База знаний IT (DokuWiki)».
- **Время:** 2026-05-21 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** карточка в `useful_resources_seed.py` (slug `dokuwiki-it-kb`, категория `services`, sort 115); логотип `ProjectTP/frontend/resource-images/dokuwiki-kb.png`; миграция `z5a6b7c8d9e0_dokuwiki_it_kb_resource.py` для существующих БД.
- **Результат:** после `alembic upgrade head` карточка появляется на вкладке «Полезные ресурсы» с ссылкой на DokuWiki.
- **Артефакт:** `ProjectTP/backend/app/useful_resources_seed.py`, `ProjectTP/backend/alembic/versions/z5a6b7c8d9e0_dokuwiki_it_kb_resource.py`, `ProjectTP/frontend/resource-images/dokuwiki-kb.png`, `HANDOFF.md`
- **Статус:** success

### Шаг 176
- **Задача:** Сделать рабочую ленту «Недавняя активность» на вкладке «График».
- **Время:** 2026-05-21 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** `GET /api/activity/recent` — последние обмены дежурствами (входящие/исходящие, статусы); фронт объединяет с событием загрузки графика и локальным «Запрос замены»; обновление при загрузке графика, обменах и каждые 30 с; тест `test_activity_recent.py`.
- **Результат:** блок показывает реальные события вместо статичных подсказок.
- **Артефакт:** `ProjectTP/backend/app/routers/activity.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/tests/test_activity_recent.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/styles.css`, `HANDOFF.md`
- **Статус:** success

### Шаг 177
- **Задача:** Ручное назначение дежурств — без лимита 2 слота на человека в день.
- **Время:** 2026-05-21 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** убрана проверка `MAX_DUTIES_PER_DAY` из `POST /api/duties/batch` и `POST /api/duties/copy-range`; лимит остаётся только в `duty_generation.py` для автогенерации; обновлён `duty_generation_fairness.md`.
- **Результат:** администратор может вручную поставить одного сотрудника на 3+ слота в день; автогенерация по-прежнему распределяет справедливо с лимитом 2.
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/duty_generation_fairness.md`, `HANDOFF.md`
- **Статус:** success

### Шаг 178
- **Задача:** Добавить полезный ресурс MobiControl (управление Apple Mac).
- **Время:** 2026-06-08 (сессия диалога)
- **Кто исполнил:** backend (агент)
- **Как исполнил:** карточка в `useful_resources_seed.py` (slug `mobicontrol`, категория `admin`, sort 120); логотип `ProjectTP/frontend/resource-images/mobicontrol.png`; миграция `b2c3d4e5f6a7_mobicontrol_resource.py`.
- **Результат:** после `alembic upgrade head` карточка появляется на вкладке «Полезные ресурсы».
- **Артефакт:** `ProjectTP/backend/app/useful_resources_seed.py`, `ProjectTP/backend/alembic/versions/b2c3d4e5f6a7_mobicontrol_resource.py`, `ProjectTP/frontend/resource-images/mobicontrol.png`, `HANDOFF.md`
- **Статус:** success

### Шаг 179
- **Задача:** Независимая генерация утренних (07:00–08:00) и обычных (09:00–17:00) дежурств по ТЗ (одна кнопка, раздельные лимиты и пулы).
- **Время:** 2026-06-08 (сессия диалога)
- **Кто исполнил:** backend + frontend (агент)
- **Как исполнил:** поле `User.is_eligible_for_morning_duties` + миграция `a1b2c3d4e5f6_user_morning_duty_eligibility.py` (для существующих строк = `is_active_for_duties`); рефакторинг `duty_generation.py` — `morning_day_counts` / `regular_day_counts`, лимиты 2+2, пул утра только с флагом утренних, раздельные глобальные счётчики справедливости; `PATCH /api/admin/users/{id}/duty-status` принимает `is_eligible_for_morning_duties` (опционально, обратная совместимость); чекбокс в админке «Утренние дежурства»; обновлены `duty_generation_fairness.md`, `check_generation_constraints.py`, `test_duty_generation.py`; в `reports.py` — импорт `MORNING_SLOT_INDEXES` из `duty_slots.py` (логика аналитики без изменений).
- **Результат:** утренний дежурный может получить дневные слоты (напр. 2 утро + 1–2 день); на 07/08 при автогенерации — только сотрудники с флагом; ручное сохранение графика без лимитов (как шаг 177); макет графика не менялся.
- **Артефакт:** `ProjectTP/backend/app/models.py`, `ProjectTP/backend/alembic/versions/a1b2c3d4e5f6_user_morning_duty_eligibility.py`, `ProjectTP/backend/app/duty_slots.py`, `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/app/routers/admin_users.py`, `ProjectTP/backend/app/routers/auth.py`, `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/app/services.py`, `ProjectTP/backend/app/routers/reports.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/backend/tests/test_duty_generation.py`, `ProjectTP/backend/tests/test_reports_recent.py`, `ProjectTP/check_generation_constraints.py`, `ProjectTP/duty_generation_fairness.md`, `ProjectTP/docs/roadmap.md`, `HANDOFF.md`
- **Статус:** success
- **Следующий шаг:** на стенде выполнить `alembic upgrade head`; в админке настроить список утренних дежурных; прогнать `check_generation_constraints.py`.

