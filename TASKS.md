# ЗАДАЧИ

Легенда: `P1` - критично, `P2` - важно, `P3` - опционально/улучшение.

## Конвенция поля «Артефакт»

Если по задаче появились новые существенные файлы (код модулей, документы, чек-листы), в карточке задачи можно добавить строку **Артефакт:** с **относительными путями от корня репозитория** (несколько путей — в одной строке или списком). Дублирует то, что уже обязательно фиксируется в `HANDOFF.md` в том же шаге; правила путей — в `docs/handoff-conventions.md`.

## Бэклог

### 17) PostgreSQL вместо SQLite и миграции (Alembic)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-01); устаревший контур `ProjectTP/duty_app/` удалён (2026-04-08).
- **Описание:** Заменить SQLite на **PostgreSQL** как единственную СУБД (`DATABASE_URL` обязателен вне docker-compose). Дополнительно: Alembic, сервис Postgres в compose, отказ от SQLite в `ProjectTP/backend/app/main.py`.
- **Критерий готовности:** Приложение работает с PostgreSQL; в репозитории есть история миграций Alembic; «чистая» установка поднимает БД с актуальной схемой; путь миграции данных со старого SQLite описан или автоматизирован по согласованию (если нужен перенос существующих инсталляций).
- **Зависимости:** Нет

### 18) Разбиение монолитного `main.py`
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-01)
- **Артефакт:** `ProjectTP/backend/app/config.py`, `database.py`, `models.py`, `schemas.py`, `services.py`, `security.py`, `deps.py`, `duty_slots.py`, `ProjectTP/backend/app/routers/` (пакет), укороченный `ProjectTP/backend/app/main.py`
- **Описание:** Поэтапно вынести из `ProjectTP/backend/app/main.py` модели, сессию/движок БД, схемы Pydantic и группы эндпоинтов в отдельные модули или пакеты (например `models`, `database`, `schemas`, `routers`, `services`) так, чтобы точка входа FastAPI оставалась компактной, а внешнее поведение API и контракты UI не изменились.
- **Критерий готовности:** Структура пакетов согласована и отражена в импортах; `main.py` существенно сокращён по объёму; smoke/happy-path по ключевым сценариям API проходит; дублирования логики нет.
- **Зависимости:** Задача 17 (опционально — чтобы не переносить вынесенные модели дважды при смене СУБД; при параллельной работе согласовать порядок с исполнителем)

## P1 - Критично

### 19) Docker Engine + Compose v2 как целевой рантайм (вместо Podman)
- **Приоритет:** P1
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/docs/deploy-rocky-linux.md` (вступление), `ProjectTP/README.md`, комментарий в `ProjectTP/docker-compose.yml` (healthcheck)
- **Описание:** Зафиксировать **Docker Engine** и `docker compose` как единственный рекомендуемый способ поднятия стека на серверах и у разработчиков; обновить инструкции (`ProjectTP/README.md`, `ProjectTP/docs/deploy-rocky-linux.md`, при наличии — `deployment_and_tz_request.md`), чтобы не опираться на Podman как на целевой рантайм; при необходимости скорректировать комментарии в `docker-compose.yml` (например healthcheck) под Docker.
- **Критерий готовности:** В документации явно указан целевой стек Docker + команды запуска из каталога `ProjectTP/`; нет противоречий «только Podman»; команда может поднять приложение по обновлённой инструкции.
- **Зависимости:** Нет

### 20) Стабильность данных PostgreSQL: тома, причины «пустой БД», бэкапы
- **Приоритет:** P1
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/docs/deploy-rocky-linux.md` (§5.1, §7)
- **Описание:** Разобрать наблюдаемую потерю данных при падении/перезапуске контейнеров: именованный том `pgdata` не должен очищаться самим фактом crash; проверить типичные причины (другой `COMPOSE_PROJECT_NAME`, другой каталог/копия репозитория, root vs rootless, Podman vs Docker, отсутствие монтирования тома, `compose down -v`). Зафиксировать чеклист диагностики (`docker volume ls` / `podman volume ls`, сопоставление с `COMPOSE_PROJECT_NAME`). Добавить или обновить раздел про резервное копирование (`pg_dump` / восстановление) в документации деплоя.
- **Критерий готовности:** В репозитории есть краткий runbook: как убедиться, что используется тот же том; чего избегать; как сделать и проверить бэкап БД перед рискованными операциями.
- **Зависимости:** Нет (логически после №19 удобнее эксплуатировать, но можно параллельно)

### 21) Документация: пересборка после изменения кода и перенос на другой сервер
- **Приоритет:** P1
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/docs/deploy-rocky-linux.md` (§5, §5.1, §7), `ProjectTP/README.md`
- **Описание:** Явно описать безопасный цикл обновления: `docker compose up -d --build` без удаления тома; предупредить про `docker compose down -v`. Описать перенос на новый сервер через логический дамп PostgreSQL (`pg_dump` → `psql` / `pg_restore`), а не «копирование тома вслепую».
- **Критерий готовности:** Разделы добавлены или обновлены в `ProjectTP/docs/deploy-rocky-linux.md` и/или `ProjectTP/README.md` (и в любом восстановленном файле деплоя в корне проекта приложения); формулировки согласованы с фактическим `docker-compose.yml`.
- **Зависимости:** Нет (пересекается с №19–20; при выполнении свести дубли в один источник со ссылками)

### 22) Битрикс: передача графика дежурств + почасовое уведомление «кто дежурит»
- **Приоритет:** P1
- **Статус:** Выполнено (2026-04-15)
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py` (`POST /api/admin/notifications/duty-schedule/bitrix`, дублирование в Битрикс в `duty-upcoming/dispatch`), `ProjectTP/backend/send_schedule_bitrix.py`, `ProjectTP/backend/app/bitrix_mention.py`, `ProjectTP/backend/app/config.py`, `ProjectTP/.env.example`, `ProjectTP/docker-compose.yml`, smoke `ProjectTP/check_bitrix_message.py`, миграция `ProjectTP/backend/alembic/versions/e4f5a6b7c8d9_users_bitrix_user_id.py`
- **Описание:** Интеграция с **Битрикс24** через входящий вебхук `im.message.add`: отправка **графика на день** в заданный диалог (`BITRIX_NOTIFY_DIALOG_ID`), упоминания сотрудников по **`bitrix_user_id`**; опционально то же сообщение о «через 2 минуты дежурство» вместе с n8n. **Почасовое** «кто сейчас дежурит» в коде приложения **не зашито** — настраивается **эксплуатацией** (внешний cron или n8n по согласованию с заказчиком, вызов API/скрипта с нужным интервалом).
- **Критерий готовности:** Переменные окружения задокументированы без секретов в git; отправка графика и тестовое сообщение работают при заполненных `BITRIX_*`; happy-path: `check_bitrix_message.py` / ручной вызов эндпоинта на стенде.
- **Зависимости:** Нет

### 23) Админ: временная выдача прав администратора («когда Вова в отпуске»)
- **Приоритет:** P1
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/backend/app/models.py` (`AdminRoleAudit`), `ProjectTP/backend/alembic/versions/b7e2c0d14f2a_admin_role_audit.py`, `ProjectTP/backend/app/routers/admin_users.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, smoke `ProjectTP/check_admin_delegate.py`
- **Описание:** Дать действующему администратору возможность **назначать другого пользователя админом** (или эквивалент — делегирование без общего списка суперпользователей в коде). Продумать отзыв прав и аудит (кто кому выдал).
- **Критерий готовности:** Админ может выдать и отозвать роль admin другому пользователю через UI/API; обычный support не может сам себе выдать admin; сценарий задокументирован.
- **Зависимости:** Задача 1 (базовый админ-раздел пользователей)

### 1) Админ: редактирование пользователей
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Добавить операции администратора: удаление пользователя, сброс/смена пароля, удаление истории отчетов пользователя.
- **Критерий готовности:** Админ может выполнить все 3 операции из UI и API с проверкой роли.
- **Зависимости:** Нет

### 2) Пользователь: смена собственного пароля
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Добавить возможность для обычного пользователя менять свой пароль в личных настройках.
- **Критерий готовности:** Пользователь успешно меняет пароль после проверки старого пароля.
- **Зависимости:** Задача 1 (общая логика обновления пароля, опционально)

### 3) Пометка неактивных дежурных
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Дать администратору возможность помечать сотрудников как неактивных/отсутствующих, чтобы генератор исключал их из распределения.
- **Критерий готовности:** Неактивные пользователи не попадают в результат генерации до повторной активации.
- **Зависимости:** Нет

### 4) Исправить скачивание Excel
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Исправить поломанный/нестабильный сценарий формирования и скачивания Excel-файла end-to-end. Скачивание готового файла с сервера — только для роли **admin** (см. также задача про массовую выгрузку).
- **Критерий готовности:** После финализации отчёта Excel стабильно формируется на сервере; **админ** может скачать файл по `/exports/...` в браузере; пользователь support может заполнять и финализировать отчёт без ошибок (без обязательной выдачи ему прямой ссылки на скачивание).
- **Зависимости:** Нет

### 8) Запрос на обмен дежурствами
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Добавить запрос на обмен дежурствами с текстом уведомления: "Ревин Сергей, Иванов Владимир запрашивает обмен дежурствами с 12:00 на 14:00"; запретить обмен с самим собой.
- **Критерий готовности:** Пользователь не может создать обмен с самим собой; целевой сотрудник получает запрос/уведомление.
- **Зависимости:** Нет

### 11) Добавить утренние дежурства (07:00 и 08:00)
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Расширить расписание дежурств, добавив временные слоты 07:00 и 08:00.
- **Критерий готовности:** Генератор/UI/хранилище поддерживают новые слоты, и они видны в расписании и отчетах.
- **Зависимости:** Задача 14 (желательно предварительно проверить логику генерации)

### 14) Анализ справедливости генерации дежурств
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** `ProjectTP/duty_generation_fairness.md`; smoke: `ProjectTP/check_generation_fairness.py`
- **Описание:** Проверить текущую логику генерации дежурств ("справедливый рандом") и убедиться, что нет скрытой эвристики/модельной логики.
- **Критерий готовности:** Алгоритм документирован с ограничениями и воспроизводимыми примерами.
- **Зависимости:** Нет

### 15) Аудит хранения паролей и Excel
- **Приоритет:** P1
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** `ProjectTP/security_audit_passwords_excel.md`
- **Описание:** Проверить, как хранятся пароли и Excel-файлы; изменить логику хранения, если текущий подход слабый.
- **Критерий готовности:** Аудит безопасности завершен; необходимые изменения хранения внедрены и задокументированы.
- **Зависимости:** Нет

## P2 - Важно

### 5) Отображение текущего дежурного в реальном времени
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Показывать текущего дежурного в реальном времени без ручного обновления страницы.
- **Критерий готовности:** UI автоматически обновляет активного дежурного (polling или websocket) в заданный интервал.
- **Зависимости:** Нет

### 6) n8n: звуковое уведомление за 2 минуты
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** `ProjectTP/n8n_notification_workflow.md`
- **Описание:** Интегрировать n8n для звукового уведомления сотрудника, который начинает дежурство через 2 минуты.
- **Критерий готовности:** Backend отдаёт событие на webhook при вызове `POST /api/admin/notifications/duty-upcoming/dispatch` (при настроенном `N8N_WEBHOOK_URL`); в n8n настроен workflow по инструкции в артефакте (звук/уведомление — на стороне n8n).
- **Зависимости:** Задача 5 (рекомендуется), настройка окружения/деплоя

### 32) Уведомление следующему дежурному за 5 минут + общий чат (одно срабатывание)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-15)
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py` (`mode=upcoming_5m|start`, alias `/api/admin/notifications/duty-upcoming/5m`, `/api/admin/notifications/duty-upcoming/start`, `GET/PATCH /api/admin/notifications/settings`), `ProjectTP/backend/app/schemas.py` (`DutyNotificationDispatchOut`, `DutyNotificationSettings*`), `ProjectTP/backend/app/models.py` (`DutyNotificationSettings`), миграция `ProjectTP/backend/alembic/versions/f1a2b3c4d5e6_duty_notification_settings.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/docs/notifications-inventory.md`
- **История номера:** Ранее под **№32** велась P3-задача «подсветка строки графика для текущего пользователя»; она **выполнена** (2026-04-08), см. `HANDOFF.md`. Ниже — актуальная постановка под тем же номером.
- **Приоритет продукта:** основной запрос — **предупреждение заранее** (до начала слота) следующему дежурному и сценарий с **общим чатом**.
- **Описание:** За **5 минут до начала** следующего слота дежурства отправлять уведомление **сотруднику, который выходит на дежурство следующим** по графику. **В то же время срабатывания** (один триггер / одна логическая отправка без искусственного сдвига) дублировать или дополнять **общим чатом** дежурных: тот же текст, ссылка на чат, пост в группу — **уточнить с заказчиком** (формат: личка + пост в чат; или одно сообщение с упоминанием и блоком про общий чат). Канал (Битрикс24 и т.д.) — согласовать; при Битрикс — связка с **№22**. Не путать с **№6** (сейчас за **2** минуты до слота): интервалы и получатели **согласовать**, чтобы не спамить.
- **Критерий готовности:** Happy-path: за 5 минут до старта слота следующий дежурный получает уведомление в ЛС; в момент старта слота сотрудник получает ЛС и одновременно уходит дублирующее уведомление в общий чат; при вызове вне минуты триггера (`HH:55` для 5m, `HH:00` для start) отправка не происходит; ID чата / webhook задокументированы (секреты вне git).
- **Зависимости:** Задача **5** (кто следующий по графику); при доставке через Битрикс — **№22** и согласование с заказчиком; с **№6** — явное правило «2 мин vs 5 мин» и получатели.

### 7) Список сотрудников и редактирование на одном экране
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Улучшить экран сотрудников (логин, ФИО) и добавить редактирование на этой же странице.
- **Критерий готовности:** Админ может просматривать и редактировать логин/ФИО на одной странице управления.
- **Зависимости:** Задача 1

### 10) Исследование блокировки Push и задача на разблокировку
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** `ProjectTP/push_unblock_checklist.md`
- **Описание:** Исследовать причины блокировки Push в браузере и подготовить операционный чек-лист для сотрудников по разблокировке.
- **Критерий готовности:** Причины задокументированы; опубликован понятный чек-лист действий для сотрудников.
- **Зависимости:** Нет

### 12) Обновление терминологии и формата времени
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Заменить "смена" на "дежурство" и показывать время как `12:00`, а не диапазоны вида `12-13` там, где это требуется.
- **Критерий готовности:** Терминология и формат времени обновлены в UI и артефактах без регрессий.
- **Зависимости:** Нет

### 13) Конструктор инструкции по авторизации инструментов
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** текст инструкции и подстановка полей в `ProjectTP/backend/app/main.py` (`build_employee_exit_instruction`); UI: кнопка «Выход сотрудника» в шапке и модальное окно; smoke: `ProjectTP/check_instruction_tools.py`
- **Описание:** Инструкция для выдачи оборудования (Sokolov, BitLocker, доменная УЗ, Bitrix, Outlook, Zoom и т.д.); сотрудник техподдержки вводит ФИО, логин и временный пароль выходящего сотрудника — они подставляются в готовый текст.
- **Критерий готовности:** Есть отдельная кнопка на сайте, форма ввода ФИО/логина/пароля, сгенерированный текст можно скопировать; изменения текста инструкции — правкой функции в backend.
- **Зависимости:** Нет

### 16) Инструкция по развертыванию и запрос ТЗ
- **Приоритет:** P2
- **Статус:** Выполнено (2026-03-31)
- **Артефакт:** `ProjectTP/deployment_and_tz_request.md`
- **Описание:** Описать шаги развертывания и подготовить явный запрос на формальное техническое задание (ТЗ).
- **Критерий готовности:** Есть воспроизводимая инструкция деплоя и подготовлен чек-лист/запрос на ТЗ.
- **Зависимости:** Нет

### 24) UI: убрать у пользователей кнопку «Сохранить изменения» на начальном экране
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`
- **Описание:** На стартовом экране (график дежурств / главная для support) убрать лишнюю кнопку сохранения, если изменения не требуют явного сохранения или сохранение уже автоматическое — не ломая сценарии админа.
- **Критерий готовности:** У роли support на начальном экране нет вводящей в заблуждение кнопки; админские сценарии сохранения графика не регрессируют.
- **Зависимости:** Нет

### 25) Отчёты: убрать выбор сотрудника и кнопку «Показать» у пользователя
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/styles.css`
- **Описание:** В разделе отчётов для обычного пользователя убрать блок **выбора сотрудника** и кнопку **«Показать»**; отчёт должен открываться в контексте текущего пользователя (или по согласованному UX).
- **Критерий готовности:** Support видит только свои отчёты без лишнего выбора; админ при необходимости сохраняет расширенные сценарии (если были).
- **Зависимости:** Нет

### 26) Уведомления: ревизия и возможный отказ в пользу Битрикс
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08) — инвентаризация и правила; интеграция Битрикс — **№22** (выполнено 2026-04-15)
- **Артефакт:** `ProjectTP/docs/notifications-inventory.md`, ссылка в `ProjectTP/README.md`
- **Описание:** Проверить текущие уведомления (n8n webhook, браузер и т.д.). При успешной интеграции с Битрикс (задача 22) — решить, **удалить ли дублирующую логику с сайта** и перенести оповещения в Битрикс.
- **Критерий готовности:** Таблица «что уведомляет сейчас / что будет в Битрикс»; нет двойных спам-сценариев; отключённые пути задокументированы.
- **Зависимости:** Задача 22 (рекомендуется сначала определить целевой канал)

### 33) Инструкция при выходе: QR-код на публичную страницу с текстом
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-24)
- **Артефакт:** миграция `ProjectTP/backend/alembic/versions/n5o6p7q8r9t0_employee_exit_instruction_shares.py`, модель `EmployeeExitInstructionShare` в `ProjectTP/backend/app/models.py`, `ProjectTP/backend/app/routers/instruction.py` (`POST /api/ee_instruction/share`, `GET /p/ee/{token}`, `GET /api/ee_instruction/qr/{token}`), `ProjectTP/backend/app/config.py` (`EE_INSTRUCTION_SHARE_TTL_SECONDS`), `ProjectTP/backend/requirements.txt` (`qrcode[pil]`), `ProjectTP/frontend/index.html` / `ProjectTP/frontend/app.js` / `ProjectTP/frontend/styles.css`, прокси `ProjectTP/local_dev_proxy.py`, `ProjectTP/nginx/default.conf`, `ProjectTP/.env.example`, smoke `ProjectTP/check_instruction_tools.py`
- **Описание:** В модальном окне «Выход сотрудника» после формирования инструкции добавить действие (кнопка), которое выдаёт **QR-код** (превью в UI и **скачивание файла изображения**, например PNG, чтобы **распечатать** QR). При сканировании открывается **URL** в браузере; по нему отображается **тот же сформированный текст** инструкции (read-only HTML). Текст инструкции хранится на сервере под **неугадываемым токеном** с настраиваемым **TTL** (срок жизни ссылки; после истечения страница недоступна, сам QR как картинка остаётся прежним). Учесть, что ссылка по сути публичная — осознанный риск для строк с паролями (как при пересылке docx).
- **Критерий готовности:** Happy-path: сформировал инструкцию → получил QR → **скачал изображение** (для печати) → отсканировал с телефона → виден тот же текст; после TTL запись не отдаётся; базовый smoke или ручной чек-лист в `HANDOFF.md`; при появлении новых файлов — строка **Артефакт:** в этой карточке.
- **Зависимости:** Задача 13 (текущий конструктор и API инструкции)

### 34) Обмен дежурством с назначенными администраторами
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-24)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`
- **Описание:** Добавить возможность пользователю инициировать обмен дежурством не только с коллегами, но и с пользователями, которым назначена роль **admin** (если они участвуют в дежурствах). Уточнить ограничения: запрет обмена с самим собой, проверки доступности слота, корректные уведомления обеим сторонам.
- **Критерий готовности:** В UI/API доступен сценарий обмена с администратором; валидации не позволяют некорректные обмены; уведомления/запросы доходят адресатам; существующий сценарий обмена между обычными сотрудниками не ломается.
- **Зависимости:** Задача 8, задача 23

### 35) Генерация графика: пропуск выходных дней
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-24)
- **Артефакт:** `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/duty_generation_fairness.md`, `ProjectTP/check_generation_fairness.py`, `ProjectTP/frontend/index.html`
- **Описание:** При автоматической генерации графика исключать **выходные дни** (суббота/воскресенье) из целевого диапазона, чтобы дежурства назначались только на рабочие дни, если иное не задано явно настройками.
- **Критерий готовности:** Автогенерация по периоду не создаёт слоты на выходные; существующие ручные сценарии редактирования графика остаются рабочими; правило и исключения (если предусмотрены) зафиксированы в UI/документации.
- **Зависимости:** Задача 14 (логика генерации), задача 30 (участники в выборках)

### 36) Сохранение графика с пустыми слотами
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-24)
- **Артефакт:** `ProjectTP/backend/app/schemas.py` (`DutiesBatchSlot.user_id` опционально), `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, smoke `ProjectTP/check_swap_request.py`, `ProjectTP/check_inactive_duties.py`
- **Описание:** Добавить возможность сохранять график, где часть временных слотов остаётся **пустой** (без назначенного дежурного), чтобы администратор мог заполнить только отдельные слоты (например назначить одного дежурного) и продолжить редактирование позже.
- **Критерий готовности:** Сохранение графика не блокируется из-за незаполненных слотов; пустые слоты корректно отображаются и могут быть дозаполнены позже; отчёты/уведомления/валидации не падают на пустых назначениях.
- **Зависимости:** Задача 24 (UI сохранения), задача 30 (правила выборки дежурных)

### 28) Отчёты Excel: столбцы и подписи заголовков
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Описание:** Переработать выгрузку отчётов: столбец **«Задача»**, **«Результат»** (было описание), **«Время работы»** (минуты текстом «N минут»); шапка **дата** / **ФИО** как в макете заказчика.
- **Критерий готовности:** Новый шаблон согласован; Excel после финализации соответствует макету; старые отчёты при необходимости мигрируют без потери данных.
- **Зависимости:** Нет

### 29) Отчёты на сайте: показывать суммарное время (часы/минуты)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Описание:** В UI отчётов отображать **общую сумму** введённого времени (часы и минуты) по дню/отчёту в понятном формате.
- **Критерий готовности:** Сумма совпадает с суммой строк; граничные случаи (пустые строки, переносы) обработаны.
- **Зависимости:** Нет

### 30) Дежурства: скрыть «не в графике» из выборки + метка у админа
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Описание:** Сотрудник с **снятой галочкой участия в дежурствах** (`is_active_for_duties` / аналог) **не показывается** в списках выбора дежурных там, где это уместно. У **администратора** рядом с ФИО отображается согласованная пометка (например **«Доби свободен»** — уточнить финальный текст с заказчиком).
- **Критерий готовности:** Неактивные к дежурствам не попадают в выборки; админ видит пометку; генерация и отчёты согласованы с правилом.
- **Зависимости:** Задача 3 (флаг участия в дежурствах)

### 31) Рефакторинг: переименовать каталог приложения в `ProjectTP`
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-08)
- **Описание:** Каталог **`Проект Проектов`** переименован в **`ProjectTP`**; обновлены пути в документации, агентах, `TASKS.md`, `HANDOFF.md`, `scripts/check_agent_context.py`, `.gitignore`, `README`, деплой-доках; локальный `backend/.venv` удалён (пересоздать при необходимости).
- **Критерий готовности:** `git grep` / поиск по репо не находит старый путь (кроме истории git); приложение собирается и запускается из нового каталога.
- **Зависимости:** Нет (лучше выполнять в окно без параллельных веток)

## P3 - Опционально/Улучшение

### 32) UI: подсветка строки с временем дежурства текущего пользователя
- **Приоритет:** P3
- **Статус:** Выполнено (2026-04-08)
- **Описание:** В общем списке графика подсвечивать строку(и), относящиеся к **текущему пользователю** (например **светло-зелёным**), чтобы быстро видеть своё время дежурства.
- **Критерий готовности:** Подсветка видна в основных браузерах; контраст доступен (не только цвет); тёмная тема не ломается или отключена для этого блока.
- **Зависимости:** Нет

### 9) Позиция H1 на странице входа
- **Приоритет:** P3
- **Статус:** Выполнено (2026-03-31)
- **Описание:** Переместить `h1` выше `loginForm` на странице входа.
- **Критерий готовности:** `h1` отображается визуально над формой логина в стандартной верстке.
- **Зависимости:** Нет

### 37) Гигиена репозитория: убрать мёртвое и лишнее, без регрессий
- **Приоритет:** P2
- **Статус:** Выполнено (2026-04-24)
- **Артефакт:** удалены `ProjectTP/backend/app/routers/duties.py` (незарегистрированный дубликат), `ProjectTP/desktop.ini`, `ProjectTP/backup-2026-04-13.sql`, `ProjectTP/backup-2026-04-16.sql`; правки в `ProjectTP/docs/notifications-inventory.md`, `ProjectTP/backend/app/routers/duty_notify_dispatch.py`, `.gitignore`
- **Описание:** Провести **аудит**, затем точечно убрать: пустые/недостижимые ветки кода, дублирование, явные заглушки и недоделки без продукта, устаревшие артефакты (в т.ч. служебные файлы ОС вроде `desktop.ini`, если не нужны команде), неиспользуемые импорты и зависимости **после** проверки реальным запуском/smoke. Оптимизации (запросы к БД, горячие циклы, размер ответа, фронт — только при измеримой выгоде или очевидной безопасной микроправке). Не удалять `check_*.py` и прочие smoke-скрипты без явной метки «устарело» в коде или документации.
- **Критерий готовности:** Список удалённого/упрощённого зафиксирован шагом в `HANDOFF.md`; happy-path по ключевым сценариям (вход, график, отчёты, админ-операции, уведомления при наличии на стенде) без регрессий; тесты/линтер проекта (если настроены) зелёные.
- **Зависимости:** Нет

## Блок: модернизация интерфейса (ориентиры Ant Design и Material Design 3)

Отдельный продуктовый/UX-фокус: внешний вид и ощущение «современного» приложения без обязательного подключения тяжёлых UI-фреймворков на первом шаге.

### 38) UI: современный вид, ориентиры Ant Design + Material Design 3
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-04)
- **Артефакт:** `ProjectTP/frontend/styles.css` (CSS-переменные, тени/поверхности, типографика, фокусы), `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html` (bump версий статики)
- **Описание:** Обновить внешний слой **`ProjectTP/frontend/`** (`index.html`, `login.html`, `styles.css`, `app.js`): сетка и отступы, типографика, роли цвета (фон, поверхность, акцент, ошибка), состояния кнопок/полей, единая шапка/карточки/модалки. **Явные ориентиры по дизайн-системам:** [Ant Design](https://ant.design) (ритм 8px, нейтральная палитра, читаемые формы и таблицы) и [Material Design 3](https://m3.material.io) (surface/elevation, скругления, motion по принципам M3, опционально «динамический цвет» как идея, не как обязательный брендинг). Допустимо остаться на чистом HTML/CSS с **токенами** (CSS variables), повторяющими **паттерны** этих систем, либо по согласованию — лёгкая библиотека компонентов. **Фоновые изображения** — только из **нормальных источников** (собственные файлы в репо, или сток с явной лицензией и атрибуцией по требованию лицензии); избегать «случайных» hotlink-CDN без политики приватности.
- **Критерий готовности:** Вход и основное приложение визуально согласованы; контраст и кликабельные зоны не ухудшены; сценарии не сломаны; при использовании внешних фонов — задокументированы источник и лицензия (в комментарии/CSS или короткой строке в `HANDOFF.md`/README приложения).
- **Зависимости:** Нет (желательно выполнять после или в координации с **№37**, чтобы не смешивать «уборку» и крупный визуальный рефакторинг в одном непрозрачном diff)

### 39) Вкладка «Полезные ресурсы» (карточки со ссылками)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`, `ProjectTP/frontend/resource-images/`
- **Описание:** Добавить новую вкладку «Полезные ресурсы» с карточками: изображение, название ресурса, краткая подпись. Клик по карточке открывает внешний сайт в новой вкладке.
- **Критерий готовности:** Вкладка отображается в меню; карточки рендерятся из фронтового списка (без backend); ссылки открываются через `target="_blank"` и `rel="noopener noreferrer"`; интерфейс адаптивен.
- **Зависимости:** Нет

### 40) График: календарный режим (неделя/месяц)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Описание:** Добавить альтернативный вид графика дежурств в формате календаря (поверх текущей таблицы), чтобы проще смотреть нагрузку по дням.
- **Критерий готовности:** Пользователь может переключаться между табличным и календарным представлением; данные берутся из текущего API без регрессии существующего сценария.
- **Зависимости:** Задача 5 (realtime-блок), задача 35 (выходные)

### 41) Роли и доступы: раздельные права в админке
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Описание:** Ввести более детальные права вместо бинарного `support/admin` (например: редактирование графика, управление уведомлениями, доступ к отчётам), чтобы делегировать функции без полного admin.
- **Критерий готовности:** Есть модель/набор прав, проверка прав на API и базовый UI-экран управления правами; текущие сценарии admin не ломаются.
- **Зависимости:** Задача 23

### 42) Уведомления: редактируемые шаблоны сообщений
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-08)
- **Описание:** Вынести тексты уведомлений (ЛС/чат) в редактируемые шаблоны, чтобы менять формулировки без правок кода.
- **Критерий готовности:** Админ может задать шаблоны в UI; backend использует их при отправке уведомлений; предусмотрен fallback на дефолтный текст.
- **Зависимости:** Задача 22, задача 32

### 43) Мини-аналитика дежурств и обменов
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-08)
- **Описание:** Добавить лёгкий аналитический блок: распределение слотов по сотрудникам, незакрытые слоты, частота обменов.
- **Критерий готовности:** На отдельном экране/блоке выводятся агрегированные показатели за выбранный период; расчёты совпадают с данными графика и обменов.
- **Зависимости:** Задача 8, задача 34, задача 36

### 44) UI-токены: зафиксировать дизайн-систему Balanced
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/styles.css`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`
- **Описание:** Зафиксировать единые токены интерфейса в `ProjectTP/frontend/styles.css` (цвета, типографика, отступы, состояния кнопок) по умеренному стилю Balanced без изменения бизнес-логики.
- **Критерий готовности:** В `:root` определены и используются единые токены; визуальные стили кнопок/карточек/вкладок читаемо разделяют primary/secondary/ghost; существующие сценарии интерфейса не регрессируют.
- **Зависимости:** Нет

### 45) UI-база: унификация компонентов (btn/card/tabs/table/msg)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/styles.css`
- **Описание:** Унифицировать базовые компоненты фронта (`btn`, `card`, `tabs`, `table`, `msg`) под один визуальный ритм и плотность, без изменения API и JS-бизнес-логики.
- **Критерий готовности:** Компоненты выглядят консистентно во всех вкладках; активные/hover/focus состояния читаемы; таблицы удобны для сканирования (в т.ч. sticky-header там, где уместно).
- **Зависимости:** Задача 44

### 46) График и отчёты: улучшить иерархию действий и зон
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/styles.css`
- **Описание:** На экранах `График` и `Отчеты` визуально разделить зоны фильтров/действий и результатов (toolbar/action-bar), усилить акценты статусов и итогов.
- **Критерий готовности:** Пользователь быстрее считывает, где управлять параметрами и где смотреть результат; основные сценарии `support/admin` проходят happy-path без изменений backend.
- **Зависимости:** Задача 45

### 47) Админка: повысить читаемость длинных секций
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/styles.css`
- **Описание:** Оптимизировать вкладку `Админ`: разбиение длинных блоков на логические под-секции, выравнивание action-зон, улучшение сканируемости формы и таблицы сотрудников.
- **Критерий готовности:** Вкладка `Админ` читается блоками; основные действия видны без лишнего скролла; права и управление сотрудниками не теряют функциональность.
- **Зависимости:** Задача 45

### 48) Полезные ресурсы: клиентский поиск по карточкам
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-08)
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/styles.css`
- **Описание:** Добавить в `Полезные ресурсы` простой клиентский поиск/фильтрацию по названию и подписи карточек без backend-изменений.
- **Критерий готовности:** При вводе в поле поиска список карточек фильтруется на клиенте; ссылки и текущие карточки работают без регрессий.
- **Зависимости:** Задача 45

## Бэклог (новые требования после закрытия задач до №48)

### 49) Уведомления и время: единый эталон МСК (Europe/Moscow)
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/backend/app/duty_tz.py`, `ProjectTP/backend/app/config.py`, `ProjectTP/backend/app/duty_notification_slot.py`, `ProjectTP/backend/app/duty_notifications.py`, `ProjectTP/backend/app/notification_scheduler.py`, `ProjectTP/docker-compose.yml`, `ProjectTP/docs/duty-time-moscow.md`, `ProjectTP/docs/notifications-inventory.md`
- **Описание:** Все расчёты «когда сработало уведомление» и границы слотов относительно графика — **строго по московскому времени**, без привязки к часовому поясу пользователя. При необходимости явно зафиксировать в коде (`Europe/Moscow`) и кратко в `ProjectTP/docs/` (в т.ч. пример `TZ` в compose/deployment).
- **Критерий готовности:** Happy-path: триггеры (:55, :00 и т.д.) совпадают с ожидаемыми минутами МСК при типичном деплое; документация согласована с поведением.
- **Зависимости:** Нет

### 50) Эксплуатация уведомлений: обязательные настройки окружения и БД
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/docs/notifications-runbook.md`, `ProjectTP/docs/notifications-inventory.md`, `ProjectTP/docs/deploy-rocky-linux.md`
- **Описание:** Runbook для эксплуатации: какие переменные окружения задать (включая согласованный **`TZ`** для МСК), роль таблицы **`duty_notification_settings`** (источник cron vs n8n, флаги, `scheduler_enabled`); обновить при необходимости `ProjectTP/docs/notifications-inventory.md` и `ProjectTP/docs/deploy-rocky-linux.md`.
- **Критерий готовности:** Новый или обновлённый раздел в репозитории; команда может воспроизвести настройку без устных пояснений.
- **Зависимости:** Задача 49 (согласование формулировок про МСК)

### 51) Уведомления: исключить дубли между каналами (scheduler / cron / n8n / Битрикс)
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/docs/notifications-dedup.md`; ссылки в `ProjectTP/docs/notifications-runbook.md`, `notifications-inventory.md`, `notifications-diagnostics.md`, `roadmap.md`
- **Описание:** Зафиксировать правила и при необходимости технические предохранители: один сценарий не должен выполняться дважды из разных цепочек (встроенный планировщик, внешний cron, n8n, прямые вызовы Битрикс). Матрица «кто шлёт что» + рекомендации по отключению лишнего.
- **Критерий готовности:** Документированный регламент; на стенде при типичной конфигурации нет повторяющихся сообщений по одному событию.
- **Зависимости:** Задача 50

### 52) Приёмка: чеклист основных сценариев на стенде
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/docs/staging-acceptance-checklist.md`
- **Описание:** Зафиксировать в репозитории краткий чеклист приёмки: вход, график (таблица/календарь), отчёты, админ-операции, уведомления при наличии конфигурации (n8n/Битрикс по стенду).
- **Критерий готовности:** Есть файл с чеклистом и местом для результата прогона (или ссылка на процесс команды).
- **Зависимости:** Нет

### 53) Генерация графика: праздники и сокращённые дни
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/backend/app/duty_rf_holidays.py`, `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/backend/requirements.txt` (`holidays`), `ProjectTP/rf_calendar_for_checks.py`, `ProjectTP/check_generation_fairness.py`, `ProjectTP/check_generation_constraints.py`, `ProjectTP/duty_generation_fairness.md`, `ProjectTP/frontend/index.html`, `ProjectTP/backend/tests/test_duty_rf_holidays.py`
- **Описание:** Исключать или помечать нерабочие дни сверх субботы/воскресенья: производственный календарь РФ и/или ручной список дат в админке (уточнить минимальный объём с заказчиком).
- **Критерий готовности:** Автогенерация по периоду не создаёт слоты на выбранные нерабочие дни; правило описано в UI или документации.
- **Зависимости:** Задача 35

### 54) График: копирование периода с предыдущего месяца (шаблон)
- **Приоритет:** P2
- **Статус:** Выполнено
- **Описание:** Дать администратору возможность взять за основу уже заполненный период (например прошлый месяц) и применить как стартовую заготовку для нового периода с возможностью правок.
- **Критерий готовности:** Happy-path: скопировать → отредактировать → сохранить без потери существующих сценариев ручного редактирования.
- **Зависимости:** Нет
- **Артефакт:** `POST /api/duties/copy-range` (`duties_live.py`, `duty_copy.py`), блок «Копирование шаблона» в админке (`index.html`, `app.js`), тест `test_duty_copy_range.py`.

### 55) График и календарь: фильтры отображения
- **Приоритет:** P2
- **Статус:** Выполнено
- **Описание:** Фильтры вроде «только мои слоты», «только пустые слоты» (для админа) поверх текущих представлений таблицы и календаря.
- **Критерий готовности:** Переключение фильтра меняет вид без изменения данных API; сценарии support/admin не ломаются.
- **Зависимости:** Задача 40
- **Артефакт:** селектор «Показ» на вкладке «График» (`index.html`), логика в `app.js` (скрытие строк таблицы, перерисовка календаря из кэша без повторных GET); опция «Только пустые слоты» добавляется при `manage_duties`.

### 56) График: экспорт периода (Excel/PDF)
- **Приоритет:** P2
- **Статус:** Выполнено (Excel; PDF не реализован — при необходимости отдельная доработка)
- **Описание:** Выгрузка графика на выбранный период для рассылки (формат по согласованию: Excel, PDF или оба). Доступ по ролям — как минимум admin (уточнить с заказчиком).
- **Критерий готовности:** Файл соответствует данным в UI за тот же период; happy-path скачивание.
- **Зависимости:** Нет
- **Артефакт:** `GET /api/duties/export-period` (`duties_live.py`, `duty_export.py`), кнопка «Скачать Excel за период» в админке (даты из блока генерации); право `manage_duties`; лимит 120 дней.

### 57) Мини-аналитика: расширение метрик
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Дополнить блок аналитики: тренды по месяцам/периодам, наглядное распределение по людям, предупреждение о перегрузе отдельного сотрудника (правила — по согласованию).
- **Критерий готовности:** Показатели согласованы с сырыми данными графика и обменов; UI читаем без перегруза.
- **Зависимости:** Задача 43
- **Артефакт:** расширен `GET /api/admin/analytics/duties-swaps` (`monthly`, `overload_warnings`), UI таблица по месяцам, полоски по сотрудникам, блок предупреждений и пояснение эвристики (`reports.py`, `schemas.py`, `index.html`, `app.js`, `styles.css`).

### 58) Инструкция «выход сотрудника»: выбор блоков (чекбоксы) вместо монолита
- **Приоритет:** P2
- **Статус:** Выполнено
- **Описание:** Разбить текст инструкции на опциональные блоки (Sokolov, BitLocker, домен, Битрикс и т.д.); пользователь отмечает нужные блоки — в превью, копировании и QR/публичной странице попадает только выбранное (ранее обсуждалось как идея №27).
- **Критерий готовности:** Happy-path: выбрать подмножество блоков → текст и публичная страница совпадают; полный набор по умолчанию не регрессирует.
- **Зависимости:** Задача 33
- **Артефакт:** `employee_exit_blocks.py`, поле `blocks` в `EmployeeExitInstructionRequest`, чекбоксы в модалке (`index.html`, `app.js`), `tests/test_employee_exit_blocks.py`.

### 59) CI: линтеры и прогон тестов на PR
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Подключить минимальный CI (GitLab/GitHub и т.п. по факту репозитория): линтеры по согласованию, `pytest` для `ProjectTP/backend/tests/`, опционально smoke-скрипты.
- **Критерий готовности:** Pipeline зелёный на эталонной ветке; правила задокументированы в `README` или `docs/`.
- **Зависимости:** Нет
- **Артефакт:** корневой `.gitlab-ci.yml` (job `backend-pytest`), раздел в `README.md`.

### 60) Автотесты: расширение покрытия
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Увеличить покрытие критичных путей поверх существующих `ProjectTP/check_*.py` и `backend/tests/` (приоритет — регрессии после изменений графика, прав, уведомлений).
- **Критерий готовности:** Добавлены тесты с явными критериями; happy-path по затронутым областям.
- **Зависимости:** Нет
- **Артефакт:** `tests/test_employee_exit_blocks.py` (сборка инструкции по блокам).

### 61) Прод: health/readiness и структурированные логи
- **Приоритет:** P2
- **Статус:** Выполнено
- **Описание:** Эндпоинты готовности для оркестрации (liveness/readiness по согласованию), структурированные или единообразные логи для разбора инцидентов — объём минимальный, без избыточной конфигурации.
- **Критерий готовности:** Документация для эксплуатации; проверка health в docker/k8s или аналоге по месту.
- **Зависимости:** Нет
- **Артефакт:** `GET /api/health` (расширен), `GET /api/ready` (проверка БД), описание в `README.md`; структурированные логи — рекомендация уровня uvicorn в README без усложнения кода.

### 62) Безопасность: лимит попыток входа и политика сессий
- **Приоритет:** P2
- **Статус:** Выполнено
- **Описание:** Защита от перебора пароля (rate limit / блокировка по IP или учётной записи) и согласованная политика сессий (срок жизни cookie/JWT — по требованиям эксплуатации).
- **Критерий готовности:** Поведение задокументировано; базовая проверка сценария блокировки/отказа.
- **Зависимости:** Нет
- **Артефакт:** in-memory лимит неудачных входов (`auth.py`, 429 после порога), `SESSION_MAX_AGE_SECONDS` в `config.py` / `main.py` / `.env.example`, README.

### 63) Доступность: контраст и навигация с клавиатуры
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Пройти основные экраны (вход, вкладки, формы, модалки, таблицы): фокус, порядок tab, контраст текста и кнопок; точечные правки CSS/разметки без смены бизнес-логики.
- **Критерий готовности:** Ключевые сценарии проходимы с клавиатуры; явные регрессы устранены.
- **Зависимости:** Нет
- **Артефакт:** `:focus-visible` для кнопок, вкладок и полей ввода (`styles.css`).

### 64) UX: пустые состояния
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Явные сообщения вместо «пустой таблицы» там, где нет слотов, нет отчётов за период, нет результатов фильтра и т.д.
- **Критерий готовности:** Пользователь понимает, что данных нет, а не «сломалось»; happy-path с данными не изменился.
- **Зависимости:** Нет
- **Артефакт:** уточнён текст пустого списка отчётов (`app.js`); ресурсы уже имели сообщение при пустом поиске.

### 65) Фронтенд: модульность шаблонов
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Вынести повторяющиеся фрагменты разметки в переиспользуемые куски (без обязательного фреймворка: шаблонные строки, общие функции рендера, частичное разбиение `app.js`), чтобы снизить риск регрессий при правках.
- **Критерий готовности:** Рефакторинг без изменения API и поведения; smoke по основным вкладкам.
- **Зависимости:** Нет
- **Артефакт:** вынесен модуль сборки текста инструкции в `employee_exit_blocks.py` (backend); для фронта зафиксировано в README согласование списка блоков с чекбоксами до полного split `app.js`.

### 66) Релизы: дисциплина версий статики
- **Приоритет:** P3
- **Статус:** Выполнено
- **Описание:** Зафиксировать правило bump версий query-string для `app.js` / `styles.css` / `index.html` при каждом значимом изменении статики, чтобы избежать «старого» кэша у пользователей.
- **Критерий готовности:** Правило описано в `README` или для команды; соблюдается на следующем релизе с изменением фронта.
- **Зависимости:** Нет
- **Артефакт:** bump `?v=20260513-backlog-close` в `index.html` и `login.html`, правило в `README.md`.

### 67) Генерация графика: исправить дублирование распределения на все дни периода
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-13)
- **Артефакт:** `ProjectTP/backend/app/duty_generation.py`, `ProjectTP/check_generation_fairness.py`
- **Описание:** При автоматической генерации графика **на диапазон из нескольких дней** наблюдается ошибка: на **каждый** выбранный день получается **одно и то же** распределение по слотам (копия первого дня), вместо независимой генерации по дням согласно правилам справедливости и исключению выходных (**№35**). Нужно локализовать причину (backend `duty_generation`, вызов из API/UI, кэш состояния генератора) и исправить так, чтобы каждая дата в периоде получала корректное своё назначение (или осознанно документированное поведение, если это не баг).
- **Критерий готовности:** Happy-path: сгенерировать период ≥3 рабочих дней — назначения по слотам **различаются между днями** там, где это ожидается алгоритмом (при неоднократных запусках random — воспроизводимо через seed в тесте или детерминированный сценарий); регрессий для однодневной генерации и ручного редактирования нет; при наличии — дополнение `check_generation_fairness.py` или unit-тест на период.
- **Зависимости:** Задача 14 (документация алгоритма), задача 35 (выходные в периоде)

### 68) UI: вкладка «Админ» только для роли admin
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-14)
- **Описание:** Боковая вкладка «Админ» видна только пользователям с `role === "admin"`. Пользователи support с делегированными флагами `can_manage_*` по-прежнему видят точечные элементы `.admin-only` на графике/отчётах, но не вкладку админ-панели.
- **Критерий готовности:** У support без роли admin вкладка скрыта; у admin видна; прямой выбор вкладки не оставляет неавторизованный контент.
- **Зависимости:** Нет
- **Артефакт:** `ProjectTP/frontend/app.js`

### 69) UI: заголовок блока «Сейчас дежурит» и без чипа «Сейчас на дежурстве»
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-14)
- **Описание:** Заменить текст «Сегодня дежурит» на «Сейчас дежурит»; убрать зелёный статус под блоком дежурного.
- **Критерий готовности:** Разметка и подписи соответствуют требованию; вёрстка hero не ломается.
- **Зависимости:** Нет
- **Артефакт:** `ProjectTP/frontend/index.html`

### 70) UI: убрать кнопку «Назначить дежурство»
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-14)
- **Описание:** Удалить быструю кнопку «Назначить дежурство» из hero графика (назначение по-прежнему через таблицу для тех, у кого есть права).
- **Критерий готовности:** Кнопки нет в DOM; мёртвых обработчиков нет.
- **Зависимости:** Нет
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`

### 71) Запрос замены: уведомление в Битрикс администраторам
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-14)
- **Описание:** По нажатию «Запросить замену» отправлять в **личные сообщения Битрикс** всем пользователям с ролью **admin**, у кого в профиле задан `bitrix_user_id` (тот же входящий вебхук `BITRIX_INCOMING_WEBHOOK_URL`, без общего чата).
- **Критерий готовности:** Эндпоинт для авторизованного support/admin; рассылка в ЛС каждому `role=admin` с `bitrix_user_id`; при пустом вебхуке или ни у кого из админов нет Bitrix ID — понятная ошибка 400; happy-path тест с моком.
- **Зависимости:** Нет (опирается на существующую интеграцию №22)
- **Артефакт:** `ProjectTP/backend/app/routers/duties_live.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/backend/tests/test_duty_replacement_bitrix_notify.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`

### 72) Обмен дежурствами: подписи слотов по графику + Битрикс адресату
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-14)
- **Описание:** В форме обмена «Мой слот» — время слота из графика, если сотрудник в графике; «Слот другого дежурного» — время и ФИО занимающего слот; при создании заявки — личное уведомление в Битрикс второму участнику (при `bitrix_user_id` и вебхуке).
- **Критерий готовности:** Селекты заполняются из `/api/duties` на дату обмена; пустой график/нет своего слота — понятные состояния; заявка создаётся даже если Битрикс недоступен; happy-path тест на `notify_duty_swap_target_bitrix`.
- **Зависимости:** Нет
- **Артефакт:** `ProjectTP/backend/app/routers/duty_swaps.py`, `ProjectTP/backend/tests/test_swap_bitrix_notify.py`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`

### 73) UI: выровнять второстепенные вкладки с dashboard-оболочкой
- **Приоритет:** P3
- **Статус:** Выполнено (2026-05-18)
- **Описание:** После визуального прохода привести вкладки «Полезные ресурсы» и «Настройки пользователя» ближе к текущему dashboard-стилю главной страницы, без изменения API и сценариев.
- **Критерий готовности:** Вкладки используют единый заголовок/карточную оболочку, настройки читаются отдельными карточками, версии статики обновлены для сброса кэша; синтаксическая проверка фронта проходит.
- **Зависимости:** Нет
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`

### 74) UI: страница «Полезные ресурсы» по новому мокапу
- **Приоритет:** P2
- **Статус:** Выполнено (2026-05-18)
- **Описание:** Обновить вкладку «Полезные ресурсы» под приложенный мокап: общий layout с левым сайдбаром, карточки ресурсов, категории, поиск и правая панель быстрых списков.
- **Критерий готовности:** Текущий список ресурсов и ссылки сохранены; карточки открываются в новой вкладке; есть фильтры «Все», «Коммуникации», «Администрирование», «Мониторинг», «Сервисы»; справа отображаются «Избранные» и «Недавно открывали» без нового API.
- **Зависимости:** Задача 39
- **Артефакт:** `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`

### 75) Аналитика: обычные и утренние дежурства отдельно
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-18)
- **Описание:** Во вкладке «Админ» → «Аналитика» считать слоты 07:00 и 08:00 как утренние дежурства отдельно от остальных слотов; утренние слоты не должны попадать в счётчики обычных дежурств.
- **Критерий готовности:** KPI, месячная таблица, распределение нагрузки и риск перегруза разделены на обычные и утренние дежурства; существующие данные и ссылки API сохраняются, новых зависимостей нет.
- **Зависимости:** Задача 57
- **Артефакт:** `ProjectTP/backend/app/routers/reports.py`, `ProjectTP/backend/app/schemas.py`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/app.js`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`, `ProjectTP/docs/roadmap.md`

### 76) Полезные ресурсы: избранное и недавние (localStorage)
- **Приоритет:** P1
- **Статус:** Выполнено (2026-05-18)
- **Описание:** На вкладке «Полезные ресурсы» избранное и история открытий хранятся в `localStorage` по `user.id` без нового API; кнопка ★ на карточке, запись «Недавно» при клике, фильтр сетки по «Смотреть все…».
- **Критерий готовности:** избранное и до 5 недавних записей сохраняются между перезагрузками; разные пользователи — разные ключи; избранное пустое до явного выбора пользователем; TTL истории 30 дней.
- **Зависимости:** Задача 74
- **Артефакт:** `ProjectTP/frontend/app.js`, `ProjectTP/frontend/index.html`, `ProjectTP/frontend/login.html`, `ProjectTP/frontend/styles.css`
